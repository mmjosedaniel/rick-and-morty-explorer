interface:
  display_name: "Verify Repository"
  short_description: "Run proportional quality gates with evidence"
  default_prompt: "Use $verify-repository to select focused, milestone, or closure checks, reuse fresh evidence, and report results."
