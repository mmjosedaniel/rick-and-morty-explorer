interface:
  display_name: "Frontend Quality"
  short_description: "Reuse-first, browser-verified frontend delivery"
  default_prompt: "Use $frontend-quality to plan, implement, or review a visible frontend milestone without changing the standard non-frontend workflow."
