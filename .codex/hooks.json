{
  "description": "Non-blocking lifecycle telemetry for milestone-oriented worker and reviewer roles.",
  "hooks": {
    "SubagentStart": [
      {
        "matcher": "^(test_worker|code_worker|frontend_code_worker|milestone_reviewer|independent_reviewer|critical_reviewer)$",
        "hooks": [
          {
            "type": "command",
            "command": "python3 -c \"import pathlib, runpy, subprocess, sys; root = subprocess.check_output(['git', 'rev-parse', '--show-toplevel'], text=True).strip(); sys.argv = ['agent_flow_metrics.py', 'hook']; runpy.run_path(str(pathlib.Path(root) / '.codex' / 'metrics' / 'agent_flow_metrics.py'), run_name='__main__')\"",
            "commandWindows": "python -c \"import pathlib, runpy, subprocess, sys; root = subprocess.check_output(['git', 'rev-parse', '--show-toplevel'], text=True).strip(); sys.argv = ['agent_flow_metrics.py', 'hook']; runpy.run_path(str(pathlib.Path(root) / '.codex' / 'metrics' / 'agent_flow_metrics.py'), run_name='__main__')\"",
            "timeout": 5,
            "async": true
          }
        ]
      }
    ],
    "SubagentStop": [
      {
        "matcher": "^(test_worker|code_worker|frontend_code_worker|milestone_reviewer|independent_reviewer|critical_reviewer)$",
        "hooks": [
          {
            "type": "command",
            "command": "python3 -c \"import pathlib, runpy, subprocess, sys; root = subprocess.check_output(['git', 'rev-parse', '--show-toplevel'], text=True).strip(); sys.argv = ['agent_flow_metrics.py', 'hook']; runpy.run_path(str(pathlib.Path(root) / '.codex' / 'metrics' / 'agent_flow_metrics.py'), run_name='__main__')\"",
            "commandWindows": "python -c \"import pathlib, runpy, subprocess, sys; root = subprocess.check_output(['git', 'rev-parse', '--show-toplevel'], text=True).strip(); sys.argv = ['agent_flow_metrics.py', 'hook']; runpy.run_path(str(pathlib.Path(root) / '.codex' / 'metrics' / 'agent_flow_metrics.py'), run_name='__main__')\"",
            "timeout": 5,
            "async": true
          }
        ]
      }
    ]
  }
}
