# Expose TASK-006 GraphQL Reads, Filters, and Request Logging


This ExecPlan is a living document. Maintain `Progress`, `Surprises & Discoveries`, `Decision Log`, and `Outcomes & Retrospective` as work proceeds. This document must be maintained in accordance with `PLANS.md`.


## Purpose / Big Picture


TASK-006 will turn the existing Express liveness process and migrated PostgreSQL model into the first project-owned product API. When the task is complete, a reviewer can send GraphQL list and owner-reconciled detail queries through Express, observe schema-derived resolver types and explicit PostgreSQL-backed projections, exercise all five required filters with deterministic literal semantics, receive stable redacted errors, and capture one bounded structured record for every completed Express request, including `/healthz` and GraphQL requests. The exact `image_url` already stored in PostgreSQL will be returned as `imageUrl`; no image bytes, asset route, proxy, or upstream character-data query will be introduced.

This plan is planning intent, not implementation evidence. At activation, `GET /healthz`, the TASK-004 migration/model foundation, and the unit, application, integration, and smoke harnesses exist. The API manifest contains no GraphQL runtime or server-side schema-code-generation dependency, `apps/api/src/app.ts` exposes only `/healthz`, `apps/api/src/server.ts` has no product composition, and no schema, generated resolver type, resolver, character read service, business repository, filter implementation, request logger, or product API test exists. The project owner authorized TASK-006 implementation on 2026-08-16, and the canonical task is now `In progress`; activation itself supplies no product behavior or acceptance evidence.


## Progress


- [x] (2026-08-16 18:39Z) Read the documentation map, exact TASK-006 record, mapped requirements, optional dispositions, accepted ADRs, routed SPEC/HS rules, current source and manifests, ExecPlan convention, milestone-slice TDD authority, worker-first workflow, and write-lease guard at clean HEAD `42d53699e9ceb5430576345d3fc3e81fe8a964aa`.
- [x] (2026-08-16 18:39Z) Confirmed that TASK-004 and TASK-017 are `Complete`, DG-006 is `Resolved`, AUTH-001 is `Authorized` within its recorded scope, TASK-005 is not a prerequisite, and TASK-006 is still `Pending` with no execution authorization or implementation evidence.
- [x] (2026-08-16 18:39Z) Created and registered this active ExecPlan, reconciled adopted optional traceability, surfaced the unresolved read-side comment allocation, and added no dependency, schema, route, test, application behavior, task-scope change, task start, gate change, or acceptance claim.
- [x] (2026-08-16 19:00Z) Ran registration validation after integrated correction: documentation validation passed for 54 Markdown files, 41 requirement IDs, 1 authorization, 18 tasks, 17 SPEC rules, 20 HS rules, and 121 scenarios; ADR validation passed for 16 ADRs and 38 requirements with only the established NFR-006 warning; `git diff --check` passed with line-ending conversion warnings only.
- [x] (2026-08-16 19:00Z) Dispositioned the first independent `REVISE`: removed the unapproved comment-read allocation and placeholder command/interfaces, assigned clean-environment proof to the first dependency/build boundary and again to the later production-startup boundary, made invalid comment pagination conditional on canonical ownership, routed Milestone 2 as one aggregate `PARTIAL` cycle, and documented the closed PostgreSQL prerequisite and ownership-safe cleanup.
- [x] (2026-08-16 19:04Z) Fresh independent final-state registration review returned `PASS` with no Blocker, Major, or Minor on plan SHA-256 `6847F6668139E7E2764F9BBBA7780A9E62A5E894B8C09A6816AD45625E6F9E8E`; only the owner reconciliation and separate execution authorization remain as intentional pre-execution joins.
- [x] (2026-08-16 21:15Z) Revised the registered plan at the project owner's direction to preserve the accepted agentic workflow while removing the standalone dependency milestone, localizing the comment-ownership stop, combining detail with its error contract, requiring server-side schema-derived resolver types, covering every Express request in logging, and adding an affected-test relevance check at each milestone join. No implementation or task state changed.
- [x] (2026-08-16 21:28Z) Synchronized the canonical TASK-006 contract, SPEC-013, HS-006, HS-014, specification routing, active-plan index, and append-only execution chronology. Documentation validation passed for 54 Markdown files and 123 scenarios; ADR validation passed for 16 ADRs and 38 mapped requirements with only the established NFR-006 warning; `git diff --check` passed with line-ending conversion warnings only.
- [x] (2026-08-16 21:35Z) Fresh independent review first identified two Minor traceability issues, then returned `PASS` with no Blocker, Major, or Minor after the canonical task folded setup into the summary slice and HS-014 evidence was split accurately between Milestones 3 and 4.
- [x] (2026-08-17 01:29Z) Received the project owner's explicit TASK-006 execution authorization; re-inspected clean branch `agent/task-006-graphql-reads` at HEAD `21f2b5c0dc0fb5f4b11e2bf80da8810c5cbbb8a2`, found no TASK-005 branch or peer worktree and no running task-owned Docker infrastructure, and landed the evidence-linked `In progress` activation across the canonical task, current status, plan index, chronology, and this living plan before any implementation worker or lease.
- [x] (2026-08-17 01:46Z) Re-listed all 39 existing `DPL-DEC-*` IDs immediately before allocating DPL-DEC-040, reconfirmed no TASK-005 branch or peer worktree, and recorded the official-documentation-backed Yoga/GraphQL, checked-in SDL, server resolver-type generation/drift, `/graphql`, and development-only GraphiQL boundary before Milestone 1 tests or source.
- [x] (2026-08-17 02:07Z) Routed Milestone 1 as coordinator-confirmed `PARTIAL`, reused the passing one-test `/healthz` evidence, accepted a guarded five-scenario GraphQL Red after one read-only packet correction, and closed Red lease `TASK-006-20260816-01-M1-red-02` compliant. Guarded Green lease `TASK-006-20260816-01-M1-green-01` then added only the exact five DPL-DEC-040 direct pins, checked-in schema and generated resolver types, thin handler/resolver/application interfaces, and `app.ts` injection seam; the frozen focused test passed 5/5 and `graphql:check` passed.
- [x] (2026-08-17 02:13Z) Completed a no-write compatibility audit at the strict-typecheck stop. Both API-only `--skipLibCheck` and `--strictBuiltinIteratorReturn false` probes removed the five `lru-cache` declaration errors while preserving the same five local Yoga/Express errors. TypeScript documents the former as skipping declaration-file checks while still checking source-referenced types and the latter as disabling a member of the strict family, so the smallest recommendation is an API-local declaration-check exception; it remains unadopted pending explicit owner authorization.
- [x] (2026-08-17 02:32Z) Received explicit owner authorization for DPL-DEC-041, the API-local `skipLibCheck: true` declaration-file exception, one same-contract Green correction, and a post-MVP debt record. Re-listed all 40 prior decision IDs before allocation and limited the correction to `apps/api/tsconfig.json`, `apps/api/src/app.ts`, and `apps/api/src/transport/graphql/graphql-handler.ts`; the accepted test, schema, generated types, manifests, lockfile, and observable contract remain frozen.
- [x] (2026-08-17 02:34Z) Validated the DPL-DEC-041 authority join before worker dispatch: documentation validation passed for 54 Markdown files, 41 requirement IDs, 1 authorization, 18 tasks, 17 SPEC rules, 20 HS rules, and 123 scenarios; ADR validation passed for 16 ADRs and 38 mapped requirements with only the established NFR-006 warning; all 41 DPL IDs are unique and ordered; and `git diff --check` passed with line-ending conversion warnings only.
- [x] (2026-08-17 02:43Z) Closed attempt-2 Green lease `TASK-006-20260816-01-M1-green-02` compliant after it touched only the three authorized paths. API typecheck reduced five local errors to two related server-context errors and exited 1, so the worker stopped before focused behavior or schema drift; prior Green evidence is stale and the correction is not accepted.
- [x] (2026-08-17 02:43Z) Completed read-only coordinator triage with an in-memory TypeScript program and no repository edit: the official direct-Express Yoga form `createYoga<{}, GraphqlContext>` plus `createSchema<GraphqlContext>` compiles with zero diagnostics under the exact repository compiler settings, while attempt 2's explicit `ExpressServerContext` produces the two remaining errors.
- [x] (2026-08-17 03:03Z) Received explicit owner direction to proceed with the exhausted-budget exception. With no active lease, the primary changed only `apps/api/src/transport/graphql/graphql-handler.ts`, removing the failed Express request/response context declarations and selecting the already-proven `createYoga<{}, GraphqlContext>` direct-Express projection. No worker budget was reset and no other source, test, schema, generated, dependency, or configuration path changed in this exceptional edit.
- [x] (2026-08-17 03:03Z) Verified the post-exception candidate: API typecheck passed; focused GraphQL application passed 5/5; `graphql:check` passed; affected application passed 6/6; root typecheck passed; root build passed; exact direct pins and frozen test hash remained unchanged; relevance/marker audits found no excluded or focused tests, pending-test markers, snapshots, placeholder comments, or duplicate helper concern; and `git diff --check` passed with line-ending warnings only.
- [x] (2026-08-17 03:03Z) Fresh S2 `independent_reviewer` returned `PASS WITH FOLLOW-UPS` on relevant-tree fingerprint `E8AAA3FE8E24D4AD94A6F7D18543C8B3510D2D1059053D0C918CDF0DC06F9D4D` with no Blocker or Major. The sole Minor was this missing exceptional-edit evidence; no source correction was requested. The reviewer independently passed API/root typecheck, focused 5/5, drift checking with generated SHA-256 `69F00BC096546F885E9CDEE15D5F41E7B470BECBC97E08EE191F1F22CA078628`, affected 6/6, runtime header/redaction probes, dependency and boundary audits, and diff checking.
- [x] (2026-08-17 03:06Z) Reconciled the sole review follow-up in this living plan and the append-only execution chronology. Documentation validation passed for 54 Markdown files, 41 requirement IDs, 1 authorization, 18 tasks, 17 SPEC rules, 20 HS rules, and 123 scenarios; ADR validation passed for 16 ADRs and 38 mapped requirements with only the established NFR-006 warning; and `git diff --check` passed with line-ending conversion warnings only.
- [x] (2026-08-17 03:07Z) Bounded confirmation by the same read-only reviewer returned `PASS` with no findings and no repository change. The reviewer confirmed that the sole Minor is closed, the append-only chronology is preserved, the required authorization/reason/path/evidence are recorded, and relevant-tree fingerprint `E8AAA3FE8E24D4AD94A6F7D18543C8B3510D2D1059053D0C918CDF0DC06F9D4D` remains stable.
- [x] (2026-08-17 03:11Z) The first pre-commit cached diff check exposed GraphQL Code Generator's canonical terminal blank line, which the prior working-tree check could not inspect while the generated path was untracked. Removing it made `graphql:check` correctly fail as stale. The coordinator regenerated unchanged SHA-256 `69F00BC096546F885E9CDEE15D5F41E7B470BECBC97E08EE191F1F22CA078628` and, between leases, added only `.gitattributes` with an exact-path `-blank-at-eof` classification. `git check-attr` and cached/working-tree diff checks now pass without weakening other whitespace checks or changing source, test, dependency, schema, or generated bytes.
- [x] (2026-08-17 03:15Z) Bounded correction-loop review returned `PASS` with no finding and no repository change. The reviewer confirmed exact-path scope, unchanged canonical `0A0A` ending and generated hash, active neighboring/default whitespace checks, passing drift and diff gates, and accurate evidence on relevant-tree fingerprint `ED479BE13F96B3B4952242EAA15415FCDFD4198D78FAD97C96DA81CBB26E0753`.
- [x] (2026-08-17 03:17Z) Created reviewed exact-tree commit `e68027b29d03144f6b69e57e6f04e5c7c99f1489` (`feat: add TASK-006 GraphQL summary boundary`) from a 14-path index with no unstaged change, frozen test SHA-256 `B783298F44BE7C104A498372746D69D5905254CB63C453B4CDAACF049FCAAC07`, generated SHA-256 `69F00BC096546F885E9CDEE15D5F41E7B470BECBC97E08EE191F1F22CA078628`, and passing cached diff check.
- [x] (2026-08-17 03:19Z) The authorized detached clean checkout of exact commit `e68027b29d03144f6b69e57e6f04e5c7c99f1489` failed the decisive schema-drift gate. Exact install, direct pins, focused 5/5, affected 6/6, root typecheck, and documentation validation passed, but `graphql:check` exited 1 as stale; root build and Milestone 1 acceptance were therefore not run or claimed. With Windows `core.autocrlf=true`, Git status was clean while the generated file had CRLF SHA-256 `27D065591E31EC5248CA1661841E84E734C65968C9B518F994C120C8E099FA55` and terminal bytes `0D 0A 0D 0A` instead of canonical LF.
- [x] (2026-08-17 03:22Z) Narrowed only the existing exact generated-path attribute to `text eol=lf whitespace=-blank-at-eof`. Local `git check-attr` now reports `text: set`, `eol: lf`, and `whitespace: -blank-at-eof`; `graphql:check` and `git diff --check` pass without a generated or product byte change. The failed commit remains preserved rather than amended.
- [x] (2026-08-17 03:28Z) Bounded correction review passed the exact LF rule and failed-proof evidence but returned `REVISE` for one material advisory statement on relevant-tree fingerprint `485B3AC1016103E62A8694F5CE27233A93C66150C91263D4D2D66DBDE14F8F90`: Sequelize 6.37.8 fixes its own advisory but still declares `uuid ^8.3.2`, so it does not remediate the transitive uuid advisory. No product or repository change was made by the reviewer.
- [x] (2026-08-17 03:28Z) Corrected the living advisory claim and appended an explicit chronology correction rather than rewriting history. Sequelize 6.37.8 is now described only as the Sequelize-advisory fix; uuid remains a separate pre-production dependency-maintenance investigation. Current absence of matching calls is reachability evidence, not remediation or approved deferral.
- [x] (2026-08-17 03:31Z) Bounded confirmation returned `PASS` with no Blocker, Major, or Minor and no repository change. The reviewer confirmed the living and append-only corrections, distinct advisory dispositions, reachability/remediation boundary, no implicit post-MVP security deferral, exact three-path staged scope, no unstaged change, and passing documentation/diff gates.
- [x] (2026-08-17 03:32Z) Created follow-up commit `0659d1ee687c83aeb7fead9576ee60957544a78e` (`fix: stabilize GraphQL codegen checkout`) from the exact three-path reviewed correction and a clean primary worktree. The first failed-proof worktree registration and its exact orphaned temporary directory were removed after verifying their identity and recording the evidence.
- [x] (2026-08-17 03:35Z) The corrected detached checkout reproduced the canonical generated LF hash under Windows `core.autocrlf=true`; exact install, direct pins, schema drift, focused 5/5, affected 6/6, root typecheck, documentation/ADR validation, root build, diff checks, marker audit, exact commit identity, and clean status passed. The final byte audit nevertheless found frozen test working SHA-256 `83A6EFED6B697CE0EAA803B4890C37B125FCA41E97490ACFF2B9DFBF24D30ECC` with 223 CRLF pairs, while `git show` confirmed unchanged frozen blob SHA-256 `B783298F44BE7C104A498372746D69D5905254CB63C453B4CDAACF049FCAAC07`. The proof therefore stopped before acceptance.
- [x] (2026-08-17 03:36Z) Added only an exact `text eol=lf` rule for the frozen GraphQL application test. Local attribute inspection reports LF, the filesystem hashes again match both frozen test and generated identities, and no test, generated, schema, dependency, or product blob changed.
- [x] (2026-08-17 03:39Z) Focused correction-loop review returned `PASS` with no Blocker, Major, or Minor and no repository change. The reviewer confirmed exact-path scope, correct Windows materialization, both working/committed hashes, zero CRLF pairs, `i/lf w/lf attr/text eol=lf`, exact three-path staged scope, passing diff gates, and accurate non-accepting evidence on milestone fingerprint `858CAB7C95169145DEC521A7FD5F1FFB0B8031549FB75BC1A1CE62FE08384213`.
- [x] (2026-08-17 03:40Z) Created preserved correction commit `aa4454ceed074540d35e48945b1ba671ea24900c` (`fix: preserve frozen GraphQL test bytes`) from the exact reviewed three-path correction and a clean primary worktree, then removed only the verified prior proof worktree and its orphaned install directory.
- [x] (2026-08-17 03:43Z) Completed the authorized clean-checkout proof of exact commit `aa4454ceed074540d35e48945b1ba671ea24900c` under Node 24.18.0, npm 11.16.0, and Windows `core.autocrlf=true`. Exact install, direct pins, schema drift, focused 5/5, affected 6/6, root typecheck, documentation/ADR validation, root build, diff and marker audits, commit identity, both `i/lf w/lf` frozen hashes, and clean status all passed.
- [x] (2026-08-17 03:43Z) Accepted Milestone 1 for the Express-hosted query schema, generated resolver types, summary projection, explicit development GraphiQL boundary, and preserved liveness contract. DPL-DEC-041 remains documented post-MVP debt; the pre-existing Sequelize/uuid advisories remain separate pre-production maintenance. No AC or full NFR-003 claim advances, and TASK-006 remains `In progress`.
- [x] (2026-08-17 03:53Z) Routed Milestone 2 as coordinator-confirmed `PARTIAL` after a fresh read-only test-owner preflight. The accepted GraphQL forwarding boundary and migrated Sequelize Character model exist, while filter normalization/service coordination, a Sequelize search repository, lazy production database composition, and executable five-filter evidence are missing as one coherent gap. A healthy task-owned Compose PostgreSQL instance is isolated on `127.0.0.1:55432`; the unrelated process on port 5432 remains untouched. No test, product source, database namespace, or repository state changed during preflight.
- [x] (2026-08-17 04:01Z) Accepted and froze the Milestone 2 aggregate Red after lease `TASK-006-20260816-02-M2-red-01` closed compliant with receipt digest `480a062fdca15b3a1f2fda25a5269965ad8185df14c2e4f940d12af8bf38e89c`. Test-side strict compilation passed; the exact focused command discovered two files and three tests, then exited 1 only because `createCharacterReadService` and `createRuntimeApplication` are absent. Real migrated database `task_004_a0f9cf25124650f6` was created, populated, and removed; an exact residue check printed `TASK_006_POSTGRES_CLEAN`. Frozen unit and integration SHA-256 values are `04C0639B916061B28FE8295CF51BE64E289BF9971D7EF700FD14B29A57B3B41E` and `69F8E334D51D65F22872C98A8A72C0059E801E50BCA2ADB9C4C5F68D63B9CA61`.
- [x] (2026-08-17 04:11Z) Closed Green lease `TASK-006-20260816-02-M2-green-01` compliant with receipt digest `9481100869e854ab6d583312454a7ff82a8357fbe11f512abc31b8d8cb00c42d`. Focused PostgreSQL passed 3/3, API typecheck and schema drift passed, affected application passed 6/6, API build passed, and a built-server probe returned liveness with an unreachable database port when credentials were injected. The canonical smoke join without PostgreSQL credentials then failed before binding `/healthz` at eager `POSTGRES_RUNTIME_CONFIG_INVALID`; static inspection also found that the repository port still reused the transport filter type instead of the required normalized application type. The candidate is not accepted as Green. One same-contract correction remains limited to lazy production configuration/resource creation and the planned filter-type boundary; frozen tests and all other paths remain unchanged.
- [x] (2026-08-17 04:21Z) Closed correction lease `TASK-006-20260816-02-M2-green-02` compliant with receipt digest `1b340562ea59ed970512b248cfa1af1f0f56d30e2a7557c2498a211cb0192195`. The two assigned defects are corrected: normalized filters are distinct, and production PostgreSQL configuration/pool creation occurs only on the first character read. Focused PostgreSQL passed 3/3, API typecheck/schema drift/build passed, affected application passed 6/6, and credential-free invalid-database configuration still served `/healthz`. Canonical smoke twice reached its sole test but produced no terminal result; a primary diagnostic run proved the browser body itself succeeded through heading visibility and HTTP 200 liveness, then hung at Playwright web-server termination. The final ordinary code-worker correction budget is exhausted, so Green remains unaccepted pending shutdown/teardown diagnosis. At the owner's overnight-pause request, all leases and task processes are closed, zero run-owned database residue was confirmed, and the exact Compose services are stopped with containers/volumes preserved.
- [x] (2026-08-17 13:19Z) Resumed at the owner's direction and completed the previously blocked Milestone 2 join without a product edit. Canonical smoke passed 1/1 once Playwright was allowed to terminate only its owned Windows process trees; smoke lifecycle passed 6/6; focused PostgreSQL passed 3/3; API unit passed 56/56; API application passed 6/6; canonical root integration passed 68/68 with zero `task_004_*` residue; root typecheck and build passed. The two frozen hashes remain exact, and the primary added only exact `text eol=lf` rules for those paths to preserve their bytes in the authorized Windows clean checkout.
- [x] (2026-08-17 13:19Z) Fresh S3 critical review returned `REVISE` with two Major findings and no Blocker or Minor: the repository adds prohibited default `ORDER BY "id" ASC`, and shutdown can observe no pool before an in-flight first read creates one after the server-close callback. The reviewer otherwise passed SQL binding/schema validation, filter and URL semantics, concurrent first-read sharing, liveness isolation, cleanup, boundaries, frozen hashes, and LF scope. Milestone 2 remains unaccepted; no product/test correction, stage, commit, or clean-checkout proof follows until the owner authorizes a new regression-test path and an exhausted-budget implementation correction.
- [x] (2026-08-17 13:25Z) The persistent Milestone 2 test owner completed one no-write correction preflight and classified the shutdown gap `PARTIAL`. The minimum deterministic Red is one new `api-unit` path, `apps/api/src/runtime-composition.unit.test.ts`, run by `npm exec -- vitest run --config vitest.config.ts --project api-unit apps/api/src/runtime-composition.unit.test.ts`; it schedules an abandoned first read, shutdown before deferred initialization resolves, and exactly-once closure after resolution. Existing frozen tests remain unchanged. The proposed exceptional Green is limited to the existing runtime composition, server, and Sequelize read-repository paths plus an exact LF rule for the new frozen test if needed. Owner authorization remains required before any of those writes.
- [x] (2026-08-17 13:51Z) Received the project owner's explicit `Autorizo` for the exact correction proposal: one guarded Red may add only `apps/api/src/runtime-composition.unit.test.ts`; after Red acceptance, one exhausted-budget exceptional Green may change only `apps/api/src/runtime-composition.ts`, `apps/api/src/server.ts`, and `apps/api/src/infrastructure/database/sequelize-character-read-repository.ts`; the primary may add only an exact LF rule for the new frozen test if Windows clean-checkout identity requires it. The two prior frozen tests, dependencies, configuration, migrations, other behavior, and publication boundaries remain unchanged.
- [x] (2026-08-17 13:57Z) Accepted and froze the authorized shutdown-ownership Red after lease `TASK-006-20260816-02-M2-red-02-scope-only` closed compliant with receipt digest `b88f13b15ba8a23d5c3122200e95d32d6c4d4db5fa7064cb262dffa4d08d147b`. The lease created only `apps/api/src/runtime-composition.unit.test.ts`; root strict typecheck passed, and the exact one-file/one-scenario focused run exited 1 only because `createLazyCharacterReadServiceOwner` is absent. Frozen SHA-256 is `CDAC325DCDC0361BE7F0C4736EB8D3F513692DCF4A0E6C2B5AD9A1A239BBBF42`. The primary added only that exact path's `text eol=lf` rule between leases; attribute inspection reports LF and the filesystem hash remains unchanged. The two prior frozen hashes remain exact.
- [x] (2026-08-17 14:10Z) Closed owner-directed exceptional Green lease `TASK-006-20260816-02-M2-green-02-owner-exception` compliant with receipt digest `8e7d499e42c36e95be29d9c5f8dfa796bb883fc4a7f722e6a823b79d0f97d8e1`; only the three authorized product paths changed. The demand-lazy owner now gates post-close initialization, waits for initialization already in flight, and closes its eventual owned resource exactly once; production shutdown uses that owner, and the prohibited default SQL order is absent. The correction test passed 1/1, API unit passed 57/57, focused PostgreSQL passed 3/3, API application passed 6/6, canonical root integration passed 68/68 with zero run-owned residue, strict typecheck/schema drift/root build passed, canonical credential-free smoke passed 1/1, smoke lifecycle passed 6/6, ports 4173/4174 are free, and all three frozen hashes remain exact. This is a Green candidate only: bounded S3 correction review, exact-tree commit, authorized clean proof, and Milestone 2 acceptance remain pending.
- [x] (2026-08-17 14:20Z) Bounded S3 correction review closed both prior product Majors and returned documentation-only `REVISE` with no Blocker or Major. The reviewer independently passed no-default-order SQL, bound filters/schema validation, lazy concurrency and post-close gating, initializer-failure propagation, exactly-once in-flight resource closure, production wiring, dual close-error preservation, liveness isolation, layer boundaries, frozen hashes, and LF scope on ten-path fingerprint `9816A2B24D05999DAFB899E29DAB0343806E8A77CC8588330BD023908BAE42F1`. Its sole Minor identified this living Outcomes summary's stale statement that Milestones 2 through 4 were unstarted. The coordinator corrected only that current-state sentence; bounded documentation confirmation remains pending.
- [x] (2026-08-17 14:23Z) The same read-only reviewer confirmed the bounded documentation correction with `PASS` and no Blocker, Major, or Minor. Outcomes now distinguishes the fully joined but unaccepted Milestone 2 candidate from unstarted Milestones 3 and 4; Progress, Revision Note, and append-only chronology preserve the review identity and remaining gates. Product/test paths and all frozen hashes remained unchanged, and repeated diff inspection passed. Exact candidate staging, local commit, clean-checkout proof, and Milestone 2 acceptance remain pending.
- [x] (2026-08-17 14:33Z) Accepted Milestone 2 from exact commit `563875cf76925a1a5e790b96d3721d699759c6b1` after the authorized detached Windows clean checkout passed exact install/pins, schema drift, correction unit 1/1, focused PostgreSQL 3/3, API unit 57/57, API application 6/6, canonical integration 68/68 with zero residue, strict typecheck, root build, canonical credential-free smoke 1/1, smoke lifecycle 6/6, documentation/ADR validation, diff/marker audits, frozen LF hashes, exact commit identity, zero owned listeners, and clean status. One initial API-unit invocation run concurrently with build failed only on a Windows temporary-cache `EPERM` rename in an unrelated migration-artifact test; the identical suite passed alone without any repository change. TASK-006 remains `In progress`; this accepts only Milestone 2 and advances no canonical AC or full NFR-003 claim.
- [x] (2026-08-17 14:45Z) Accepted the Milestone 3 test-owner preflight as `UNKNOWN` at clean HEAD `32676315674df5b553b139bf35738e5c738a87db`: all-request logging is absent, and the promised pre-implementation serialization decision was also absent. Re-listed DPL-DEC-001 through DPL-DEC-041 before allocating DPL-DEC-042, which now fixes the seven-field JSON shape, `operationName: null` convention, server-owned request ID, monotonic integer duration, error count, safe string normalization and byte caps, 1024-byte LF-terminated line cap, metadata sources, and excluded content. No product source, executable test, dependency, route, configuration, service, or external state changed.
- [x] (2026-08-17 14:56Z) Accepted and froze the corrected Milestone 3 Red at SHA-256 `A0B75EBDA092064A485213FCCD97C511081C046D273F437CF8911883739533C4`. Attempt-1 lease `TASK-006-20260816-03-M3-red-01` closed compliant and proved the exact absent logger after strict typecheck passed and all three requests completed, but its short operation names did not discriminate DPL-DEC-042's untrusted-metadata bound. One same-contract test correction changed only the successful fixture to a valid 160-byte GraphQL name with an expected 96-byte projection. Attempt-2 lease `TASK-006-20260816-03-M3-red-02` closed compliant at receipt `9d5235e89f370e41d65cdfd065a9f2ce36b73712f361a40478a636abbc6ddd3b`; typecheck passed and the exact one-file/one-test run again exited 1 only at `TASK_006_REQUEST_LOG_RECORD_MISSING` after health, successful long-name GraphQL, and controlled failing GraphQL requests completed. No product or existing test changed.
- [x] (2026-08-17 15:05Z) Closed attempt-1 Green lease `TASK-006-20260816-03-M3-green-01` compliant at receipt `8413412ddc7d6f707aa2ef4cb58951c0db1be5c42d4aaadb5166e28f7579de90`. The middleware, app, Yoga handler, and server implement the fixed record, production stdout/UUID/monotonic dependencies, and request-local GraphQL metadata; focused logging passed 1/1 and affected application passed 3 files/7 tests with the frozen hash exact. Root typecheck then failed only on five new Yoga/Express projection diagnostics, so the worker correctly stopped before schema drift, unit, build, and diff joins. One same-contract attempt-2 Green may change only those four source paths to use the installed typed Yoga handle/plugin boundary; behavior, frozen tests, compiler configuration, dependencies, and all other paths remain fixed.
- [x] (2026-08-17 15:10Z) Closed attempt-2 Green lease `TASK-006-20260816-03-M3-green-02` compliant at receipt `70aacabc87ab6aae58d0602bff6d009aefd79d2a7a70353bdf1303f4708e04fc`. Explicit plugin generics resolved all four `graphql-handler.ts` diagnostics; root typecheck still exits 1 only because Express `Request` does not select Yoga's generic `.handle` Node overload in `app.ts`. The ordinary correction budget is exhausted, so the worker stopped before stale behavior/join reruns. A coordinator read-only in-memory compile proves the exact behavior-preserving projection `handleNodeRequestAndResponse(request, response, metadata === undefined ? {} : { requestLogMetadata: metadata })` yields zero API diagnostics. No disk edit applied that probe. One exceptional owner authorization for only `app.ts` is now required before validation may resume.
- [x] (2026-08-17 15:32Z) Received the project owner's explicit `autorizo` for the exact exceptional `app.ts` adapter correction. The authority permits only the already proven `handleNodeRequestAndResponse` call and exact-optional omission of absent request-log metadata; it does not reset either ordinary correction budget or permit a test, behavior, path, dependency, configuration, schema, or documentation-scope expansion. The primary will apply and record this one-file exceptional edit, then rerun every stale Green and milestone join before S3 review or acceptance.
- [x] (2026-08-17 15:36Z) Applied the exact authorized `app.ts` projection, passed root typecheck, and immediately stopped when the focused request-log test timed out at five seconds because the method returns a Fetch response without sending it to Express. Restored the prior behaviorally passing `.handle` candidate. Local implementation inspection and read-only strict probes now prove the overload accepts Express when absent metadata is omitted: retaining `.handle` and passing `metadata === undefined ? {} : { requestLogMetadata: metadata }` yields zero diagnostics. That corrected one-file route remains unapplied because it differs from the explicitly authorized method; all leases are closed, no process/service remains, and the frozen test hash is exact.
- [x] (2026-08-17 15:42Z) Received standing project-owner authority for bounded TASK-006 corrections, including the revised one-file `.handle` exact-optional correction, and for explicit post-MVP disposition of issues that are both non-blocking and safe for future implementation. This authority does not reset ordinary worker budgets, expand product scope, waive mandatory/security/correctness/closure findings, or permit push, PR, deployment, or live-upstream activity. Every exceptional correction still requires recorded path, reason, and validation; every deferred item requires `Critical`, `Medium`, or `Low` classification, rationale, and exit criterion, with Critical items requiring case-specific owner review. Classified DPL-DEC-041 as existing `Medium` compatibility debt without changing its behavior or exit criterion.
- [x] (2026-08-17 15:50Z) Applied the standing-authorized `app.ts` correction directly: the established Yoga `.handle` route now receives `{}` when request-log metadata is absent and `{ requestLogMetadata }` otherwise. Root typecheck, focused request logging 1/1, affected API application 7/7, schema drift, repeated API unit 57/57, root build, focused PostgreSQL 1/1, canonical integration 68/68 with zero `task_004_*` residue, credential-free smoke 1/1, smoke lifecycle's three scenarios, exact frozen hash, and diff checking pass. The first API-unit aggregate invocation failed only its historical TASK-004 default-entry observation group; the exact isolated test passed 1/1 and the identical full suite passed 57/57 without a repository change, so the repeated terminal full-suite result is current evidence and the transient remains disclosed for review. Smoke emitted bounded production `/healthz` records to standard output, ports 4173/4174 are free, and the task-owned Compose services are stopped with containers and volumes preserved. Milestone 3 is a joined Green candidate pending fresh S3 review, evidence reconciliation, exact commit, and acceptance.
- [x] (2026-08-17 16:05Z) Fresh S3 critical review returned `REVISE` with one Major and no Blocker or Minor on eight-path fingerprint `7E5358AD4CE71C8FB34DABF6520A9032C6EF7E56FB4E25C1766ACF38AA9FB39D`. Product inspection and independent probes passed exactly-once finish logging, request-local concurrency, GraphQL parse/selection/execution error metadata, fixed ordering/LF, all normalization and numeric bounds, a 946-byte worst-case escaped line, excluded-content safety, production wiring, liveness, and scope. The sole Major is deterministic Windows identity: the new frozen test has no exact `text eol=lf` attribute while `core.autocrlf=true`. Standing owner authority permits the smallest primary integration correction in `.gitattributes`; product and test bytes remain frozen. The transient TASK-004 unit observation requires no debt entry because unchanged isolated and aggregate reruns passed. Sink-write failure is unspecified and therefore not a defect or fail-open claim; several serializer edge cases remain unautomated regression risks but passed bounded product probes and are not review findings.
- [x] (2026-08-17 16:07Z) Applied only the reviewed `.gitattributes` correction. `git check-attr` now reports `text: set` and `eol: lf` for the frozen request-log test; its filesystem contains zero CRLF pairs and retains SHA-256 `A0B75EBDA092064A485213FCCD97C511081C046D273F437CF8911883739533C4`; `git diff --check` passes with conversion warnings only. No product, test, dependency, schema, generated, or runtime byte changed. Bounded S3 confirmation and the exact committed Windows clean proof remain before Milestone 3 acceptance.
- [x] (2026-08-17 16:10Z) The same read-only S3 reviewer confirmed the bounded LF correction with `PASS` and no Blocker, Major, or Minor. The exact path rule is effective without weakening neighboring or default whitespace behavior; the frozen test has zero CRLF pairs and exact hash; all five product/test hashes remain unchanged; documentation preserves the original fingerprint, append-only correction chronology, sink-failure nonclaim, transient-unit disposition, and remaining gates. Current eight-path fingerprint after intended documentation updates is `632108775EEC7B1798B3E72E873DA2EEDAA62BD2C8C5DC3EB56F79C9A433ECD6`. Exact primary staging, candidate commit, Windows clean proof, and Milestone 3 acceptance remain.
- [x] (2026-08-17 16:12Z) Accepted Milestone 3 from reviewed local commit `14ae00b40ce915e784535a0e78e4cc7f1271e9e8`. The exact current tree passes post-commit typecheck and focused logging 1/1; all earlier focused/affected/schema/unit/build/PostgreSQL/smoke/lifecycle gates remain reusable because no product or test byte changed after their terminal runs. Detached Windows checkout `C:\Users\mmjos\AppData\Local\Temp\rick-and-morty-task006-m3-14ae00b` at `core.autocrlf=true` reproduced `i/lf w/lf attr/text eol=lf`, zero CRLF pairs, and frozen SHA-256 `A0B75EBDA092064A485213FCCD97C511081C046D273F437CF8911883739533C4`; commit/diff/documentation/ADR/clean-status checks passed. The verified checkout was removed after its identity was recorded. This accepts only the bounded all-Express request record and creates an AC-011 evidence candidate; TASK-006 remains `In progress`, no canonical AC or full NFR-003 claim advances, and Milestone 4 remains owner-blocked.
- [ ] Obtain project-owner reconciliation of the TASK-006/TASK-008 bounded-comment ownership ambiguity and update the canonical task/specification routing before Milestone 4, without blocking Milestones 1 through 3.
- [ ] Complete owner-reconciled Milestone 4 for the detail projection, identifier semantics, stable errors, and server/client diagnostic separation.
- [ ] Complete Milestone 5 closure: full semantic test-relevance audit, one authoritative closure packet, risk-routed integrated review, documentation-impact reconciliation, and the task-closure documentation gate.
- [ ] Only after every closure join passes, change TASK-006 to `Complete`, record any evidence-based readiness change without overstating full NFR-003 or overall product acceptance, move this stable plan to `docs/plans/completed/`, repair links, and append closure chronology.


## Surprises & Discoveries


- Observation: TASK-006 became owner-authorized and `In progress` through one coherent activation change, while its prerequisites and bounded authorization remain unchanged. Activation did not add a dependency, schema, route, test, or runtime claim.
  Evidence: `docs/IMPLEMENTATION_PLAN.md#task-status-and-dependency-index`, `docs/IMPLEMENTATION_PLAN.md#task-006---expose-graphql-reads-filters-and-request-logging-through-express`, and the 2026-08-16 activation row in `docs/execution/decision-and-progress-log.md`.
- Observation: The activation baseline has no separate TASK-005 branch or peer worktree, and the Docker engine is unavailable, so no running local Compose project is owned by TASK-005 or TASK-006. Milestone 2 infrastructure remains a future environment prerequisite and cannot reuse activation evidence.
  Evidence: `git worktree list --porcelain`, `git branch --all --list '*task-005*' '*task-006*'`, and `npm run infra:ps` on 2026-08-17 01:29Z.
- Observation: Current `graphql-yoga` 5.21.2 supports Node 24 and direct Express middleware but peers only GraphQL 15 or 16. Current GraphQL 17.0.2 is therefore incompatible with the selected adapter; GraphQL 16.14.2 is the latest compatible 16.x patch.
  Evidence: official Yoga Express/GraphiQL/Node documentation and read-only npm registry metadata checked on 2026-08-16, recorded in DPL-DEC-040.
- Observation: The official Codegen server preset adds module conventions and dependencies intended for larger modular schemas. This milestone needs one schema and one resolver-signature output, so the direct CLI plus TypeScript and TypeScript-resolvers plugins is the smaller complete boundary.
  Evidence: official GraphQL Code Generator server-preset, plugin, local-schema, ESM, and `--check` documentation checked on 2026-08-16, recorded in DPL-DEC-040.
- Observation: The focused GraphQL contract and deterministic schema drift pass, but the first strict join exposed a dependency compatibility gap that registry metadata alone did not reveal. `graphql-yoga` 5.21.2 declares `lru-cache ^10.0.0`, npm resolves 10.4.3, and that package's `Generator<..., void>` map-iterator declarations do not satisfy TypeScript 6's strict `MapIterator<...>` return contract. The same join also found local `Record<string, never>` Yoga generics and untyped Express middleware parameters that remain ordinary same-contract correction work.
  Evidence: `npm run typecheck` on 2026-08-17 02:03Z, `npm ls lru-cache --all`, and the installed `graphql-yoga` 5.21.2 and `lru-cache` 10.4.3 manifests. The application join passed 6/6 and `graphql:check` passed on the unchanged candidate tree.
- Observation: A command-line-only API compile with `--skipLibCheck` removes only the third-party declaration failures and still reports every local implicit-`any` and adapter-generic error. The same probe with `--strictBuiltinIteratorReturn false` also removes the dependency failures, but TypeScript classifies that option inside `strict`; disabling it would weaken source iterator checking. The installed `lru-cache` 11 declaration avoids the `Map` implementation conflict, but forcing it would cross Yoga's declared `^10.0.0` dependency range rather than repair the selected supported graph.
  Evidence: `npm exec -- tsc --noEmit --project apps/api/tsconfig.json --skipLibCheck`, the equivalent `--strictBuiltinIteratorReturn false` probe, the [TypeScript `skipLibCheck` reference](https://www.typescriptlang.org/tsconfig/skipLibCheck.html), the [strict iterator-return reference](https://www.typescriptlang.org/tsconfig/strictBuiltinIteratorReturn.html), and the installed Yoga/lru-cache manifests and declarations on 2026-08-17 02:13Z.
- Observation: The owner authorized the smallest API-local declaration-file exception and requested that its compatibility cost remain visible for possible post-MVP removal. DPL-DEC-041 therefore makes the exception explicit technical debt with a falsifiable exit: delete the flag and pass root typecheck, build, and affected TASK-006 tests on a supported dependency graph.
  Evidence: the owner's 2026-08-16 authorization, DPL-DEC-041, and `docs/IMPLEMENTATION_PLAN.md#deferred-post-mvp-engineering-follow-ups`.
- Observation: The bounded attempt-2 worker modeled Express request/response as Yoga's mandatory server context. That makes Yoga's optional third callable argument incompatible with Express's `NextFunction` and also makes the schema require request/response fields. Yoga's official direct-Express integration does not use that server-context generic; an in-memory strict-TypeScript probe confirms that `{}` as the server context and `GraphqlContext` as the user context compiles directly with the existing Express mount.
  Evidence: API typecheck after lease `TASK-006-20260816-01-M1-green-02`, installed Yoga server-adapter declarations, the official Express integration, and the zero-diagnostic in-memory variants `directEmpty`, `directDefault`, and `wrappedEmpty` on 2026-08-17 02:43Z.
- Observation: The owner-authorized exceptional edit produced the same zero-error type result predicted by the no-write probe, and every observable M1 check remained Green. Fresh S2 review found no implementation defect; its sole Minor was that this living evidence still described the pre-authorization stop.
  Evidence: the owner's `go ahead` direction, the one-file `graphql-handler.ts` diff, post-exception API/root typecheck and 5/5/6/6/drift/build results, and the `PASS WITH FOLLOW-UPS` review on fingerprint `E8AAA3FE8E24D4AD94A6F7D18543C8B3510D2D1059053D0C918CDF0DC06F9D4D`.
- Observation: GraphQL Code Generator 7.2.0's canonical resolver output ends with a blank line. This is visible to the cached diff only after the formerly untracked generated file is staged; deleting it makes the required drift check fail. One exact `.gitattributes` rule can classify only that canonical terminal line while leaving all other Git whitespace checks active.
  Evidence: initial `git diff --cached --check` exit 2 at generated line 146, deliberate one-line removal followed by `graphql:check` exit 1 as stale, regeneration to unchanged SHA-256 `69F00BC096546F885E9CDEE15D5F41E7B470BECBC97E08EE191F1F22CA078628`, `git check-attr whitespace` reporting `-blank-at-eof`, and cached/working-tree diff checks exiting 0.
- Observation: An exact Windows clean checkout can be Git-clean yet fail Codegen drift because `core.autocrlf=true` materializes the canonical LF-generated file as CRLF. Whitespace classification alone does not control the working-tree bytes that Codegen hashes; the same exact path must also declare `text eol=lf`.
  Evidence: failed detached proof of `e68027b29d03144f6b69e57e6f04e5c7c99f1489`, clean status, working hash `27D065591E31EC5248CA1661841E84E734C65968C9B518F994C120C8E099FA55`, terminal `0D 0A 0D 0A`, canonical hash `69F00BC096546F885E9CDEE15D5F41E7B470BECBC97E08EE191F1F22CA078628`, and local passing `text eol=lf` attribute/drift checks after the correction.
- Observation: The same Windows checkout conversion changes the frozen Red test's filesystem SHA-256 even when the committed Git blob and behavior remain exact. A clean proof that promises the recorded byte identity must control that one test's materialization independently of broader repository line-ending defaults.
  Evidence: exact commit `0659d1ee687c83aeb7fead9576ee60957544a78e`; all executable gates passing; Git-clean frozen working SHA-256 `83A6EFED6B697CE0EAA803B4890C37B125FCA41E97490ACFF2B9DFBF24D30ECC` with 223 CRLF pairs; Git-blob SHA-256 `B783298F44BE7C104A498372746D69D5905254CB63C453B4CDAACF049FCAAC07`; and local exact-test `text eol=lf` inspection with restored filesystem hash.
- Observation: The exact clean install reports high-severity Sequelize advisory `GHSA-6457-6jrx-69cr` for 6.37.7 and moderate uuid advisory `GHSA-w5hq-g745-h8pq` for transitive 8.3.2. Sequelize 6.37.8 addresses only the Sequelize advisory; its official manifest still declares `uuid ^8.3.2`, so the uuid advisory requires a separate compatible remediation investigation. These dependencies predate TASK-006, and current repository source has no JSON/JSONB cast/literal or direct external-buffer uuid v3/v5/v6 call matching the reported vulnerable operations.
  Evidence: `npm audit --json` against the exact committed lockfile, scoped source/dependency inspection, the [Sequelize advisory](https://github.com/sequelize/sequelize/security/advisories/GHSA-6457-6jrx-69cr), the [uuid advisory](https://github.com/advisories/GHSA-w5hq-g745-h8pq), and the [Sequelize 6.37.8 manifest](https://raw.githubusercontent.com/sequelize/sequelize/v6.37.8/package.json) on 2026-08-17. TASK-006 does not silently widen into the TASK-004 persistence dependency; separate dependency-maintenance authorization and affected migration/TASK-004/TASK-006 validation are required before production delivery. Current reachability evidence is not remediation, and this is not classified as additional post-MVP debt.
- Observation: The default PostgreSQL port is occupied by an unrelated local process, while Docker Desktop and the repository-owned Compose profile are available. Milestone 2 can therefore use the same closed loopback profile on free port 55432 without stopping or reconfiguring the unrelated service. A first cold TASK-004 persistence preflight exceeded its fixed five-second test timeout and left one exactly identified run-owned database; the coordinator dropped only that database, an identical warm rerun passed 1/1, and a residue query returned zero `task_004_*` databases.
  Evidence: Docker server 29.7.2, healthy Compose container `5586c564678c` from project `rick-and-morty-dev` at `127.0.0.1:55432`, exact run-owned database cleanup, passing warm persistence command, and the zero-residue query on 2026-08-17. The cold timeout is environment warmup evidence, not a behavioral Red or reusable Milestone 2 Green result.
- Observation: The full real-PostgreSQL Red can reach migration and fixture setup before failing at the absent production boundary, so it proves both that the infrastructure prerequisite is usable and that the missing contract is isolated. The unit Red independently proves the absent normalization/service factory. Both tests compile strictly, and the integration test preserves operation, connection-close, namespace-cleanup, and residue-check failures rather than hiding cleanup damage.
  Evidence: closed-compliant Red receipt `480a062fdca15b3a1f2fda25a5269965ad8185df14c2e4f940d12af8bf38e89c`, exact focused exit 1 across three tests, `TASK_006_RUNTIME_COMPOSITION_MISSING`, two missing-factory failures, namespace `task_004_a0f9cf25124650f6`, and `TASK_006_POSTGRES_CLEAN` on 2026-08-17.
- Observation: Constructing a Sequelize instance is connection-lazy, but validating mandatory database credentials at module evaluation is still application-startup eager. The focused database path and an unreachable-port probe can pass while canonical credential-free liveness fails, so both the task smoke and code inspection are necessary before Green acceptance.
  Evidence: attempt-1 Green receipt `9481100869e854ab6d583312454a7ff82a8357fbe11f512abc31b8d8cb00c42d`; passing focused/type/drift/application/build and injected-config probe; canonical `npm run test:smoke` failure at `POSTGRES_RUNTIME_CONFIG_INVALID` before `/healthz`; and no listener residue on ports 4173/4174 after exact task-owned cleanup on 2026-08-17.
- Observation: After deferring PostgreSQL configuration correctly, canonical smoke executes its complete browser assertions but does not terminate its managed web-server processes. Debug evidence reaches successful page navigation, heading visibility, `GET /healthz` HTTP 200, browser/context closure, and then stops at `pw:webserver Terminating the WebServer`. This is a teardown/shutdown stop, not evidence that the smoke behavior failed or passed as a command.
  Evidence: correction receipt `1b340562ea59ed970512b248cfa1af1f0f56d30e2a7557c2498a211cb0192195`; two worker smoke reproductions; primary `DEBUG=pw:webserver,pw:api` reproduction; explicit interruption of task-owned session 9540; and zero listeners on ports 3000, 3107, 4173, and 4174 afterward. The command lacks a decisive exit, so Milestone 2 Green and review are not claimed.
- Observation: The non-terminal smoke was a Windows sandbox teardown-permission artifact, not an API lifecycle defect. The browser case completed, but Playwright could not execute its owned-tree `taskkill`; the same canonical command and the six-case lifecycle matrix terminate normally when granted that exact process permission.
  Evidence: sandboxed `DEBUG=pw:webserver` process-tree inspection, exact owned-tree termination, canonical `npm run test:smoke` exit 0 with 1/1, `npm run test:smoke:lifecycle` exit 0 with 6/6, and no listener residue on ports 4173/4174 on 2026-08-17.
- Observation: Package-scoped API integration is not the canonical root integration gate because npm changes the effective directory to `apps/api`, where root `migrate:*` scripts are intentionally absent. The authoritative root command retains the repository root and passes all registered migration and TASK-006 tests.
  Evidence: `npm run test:integration --workspace @rick-and-morty/api` produced nine code-1/empty-output root-CLI mismatches with complete namespace cleanup; `npm run test:integration` from the repository root then passed 6 files and 68/68 tests with zero `task_004_*` residue on the same source and PostgreSQL identity.
- Observation: Demand-lazy initialization needs explicit shutdown ownership beyond a single post-`server.close` pool check. A client-aborted first GraphQL request can let the close callback observe no resource, after which the suspended initializer can create a pool that no remaining path closes.
  Evidence: fresh S3 review of `server.ts`, an independent loopback ordering probe reporting `callbackBeforeResource:true` and `resourceCreatedAfterClose:true`, and the absence of production-entrypoint signal coverage in the frozen externally injected PostgreSQL test. The same review also found the unnecessary prohibited `ORDER BY "id" ASC` clause.
- Observation: The automatic guard prevented two procedural defects from contaminating implementation evidence: the first Red packet stopped read-only on the invalid `NFR-BE-003` token before writing, and the corrected packet used authoritative `NFR-003`; every terminal Red/Green receipt was compliant with no unleased or forbidden path.
  Evidence: terminal leases `TASK-006-20260816-01-M1-red-01`, `TASK-006-20260816-01-M1-red-02`, and `TASK-006-20260816-01-M1-green-01`.
- Observation: TASK-005 may run in parallel but is not a TASK-006 prerequisite. Real PostgreSQL tests can create deterministic characters and, if owner-assigned, comments directly in a run-owned migrated namespace, so this plan must neither wait for the importer nor call the live public Rick and Morty API.
  Evidence: the TASK-006 prerequisite text in `docs/IMPLEMENTATION_PLAN.md`, `vitest.config.ts`, and `apps/api/src/infrastructure/database/postgres-lifecycle.ts`.
- Observation: the current liveness behavior is `EXISTING_AND_COVERED`; manufacturing a Red for it would violate ADR-0016. The product GraphQL boundary is absent, while the absence of image asset/proxy routes is existing but not directly covered.
  Evidence: `apps/api/src/app.ts`, `apps/api/src/app.application.test.ts`, and the absence of GraphQL packages or source in `apps/api/package.json` and `apps/api/src/`.
- Observation: At registration, FR-BE-006 and AC-011 required request middleware for each request while SPEC-013 supplied only a GraphQL example. Derived examples could not narrow mandatory scope, so this revision synchronized SPEC-013 and HS-014 around an Express-wide logger that proves at least `/healthz` plus GraphQL success and failure without changing liveness into readiness.
  Evidence: `docs/REQUIREMENTS.md`, revised SPEC-013 and HS-014, `docs/SYSTEM_DIAGRAM.md`, and the former GraphQL-only wording in this plan.
- Observation: ADR-0002 requires GraphQL resolver types, not only future frontend operation types, to be generated from the version-controlled schema. The former standalone dependency milestone omitted that accepted build contract.
  Evidence: ADR-0002 and `docs/SYSTEM_DIAGRAM.md`; the current API manifest has no GraphQL code-generation dependency or command.
- Observation: the task records split comment reads ambiguously. TASK-006 promises a detail query whose ADR-0006 projection includes bounded comments, while TASK-008 explicitly owns deterministic comment ordering and pagination. An ExecPlan cannot change either task's scope, so the conflict requires owner-controlled canonical reconciliation before the detail/error Red and task closure.
  Evidence: ADR-0006, SPEC-008, HS-008, HS-009, and the TASK-006/TASK-008 records in `docs/IMPLEMENTATION_PLAN.md`.
- Observation: ADR-0016 and the implementation roadmap stop only work dependent on `CONFLICTING` evidence. Summary, list/filter, and Express-wide logging do not allocate comment reads, so the unresolved detail join must not block their milestones.
  Evidence: ADR-0016's `CONFLICTING` route and `docs/IMPLEMENTATION_PLAN.md#roadmap-role-and-ai-assistant-execution-model`.
- Observation: publishing ADR-0006's complete schema before its mutation owner runs would expose unusable fields. TASK-006 therefore stages a query-only checked-in schema, and TASK-008 adds `Mutation` after its own Red. This is incremental implementation of one accepted contract, not a competing schema.
  Evidence: ADR-0006's schema and the canonical statement that TASK-006 owns queries while TASK-008 owns mutations.
- Observation: adding product database composition must not turn `/healthz` into readiness or make the TASK-003 smoke require PostgreSQL. Runtime composition must be lazy: API startup and `/healthz` do not authenticate PostgreSQL, apply migrations, import data, or query a store.
  Evidence: ADR-0011's smoke boundary, `docs/IMPLEMENTATION_PLAN.md#task-003---establish-the-operational-walking-skeleton`, and current `apps/api/src/server.ts`.
- Observation: TASK-006's local mapped-scope line omitted adopted OR-001, OR-007, and OR-008 even though the optional-disposition and requirement-coverage owners assign strict TypeScript, character-search unit coverage, and the selected use-case/service boundary to this work. Registration repairs that trace only; OR-003 and OR-004 remain frontend-owned.
  Evidence: `docs/adrs/README.md#optional-scope-decisions` and `docs/IMPLEMENTATION_PLAN.md#requirement-to-task-coverage`.
- Observation: the GraphQL runtime, schema, server-side type generation, Express handler, and summary projection share one first observable transport contract. Installing dependencies in a separate zero-yield milestone would create an extra clean-environment and review barrier without adding a requirement outcome; dependency/setup work belongs to the minimum Green for that transport slice.
  Evidence: the plan-implementation vertical-outcome rule, ADR-0016's coherent-slice definition, and the former Milestone 1 acceptance yield of no SPEC, HS, FR, or AC.
- Observation: Milestone 1 cannot combine a GraphQL Red with a separate passing-characterization write under one ADR-0016 assignment. The coherent route is coordinator-confirmed `PARTIAL`: reuse existing `/healthz` coverage, inspect image/proxy absence as negative scope, and Red only the missing GraphQL summary contract.
  Evidence: ADR-0016 and `.codex/execplan-implementation-workflow.md` require exactly one preflight classification and one `evidence` or `red` test phase per milestone packet.


## Decision Log


- Decision: Register the plan without starting TASK-006.
  Rationale: Plan creation is documentation evidence only. The worker-first flow activates only after separate owner authorization and canonical `In progress` state.
  Date/Author: 2026-08-16 / Codex primary coordinator.
- Decision: Treat TASK-006 as an implementation plan, not consequential decision work; omit a Decision Review Contract.
  Rationale: DG-006 is already resolved and AUTH-001 is already Authorized. The remaining package, route, record-shape, and any owner-assigned ordering choices are reversible execution decisions recorded in the global decision log, not a new ADR or gate.
  Date/Author: 2026-08-16 / Codex primary coordinator.
- Decision: Before owner reconciliation, freeze only the undisputed query subset: `characters`, `CharacterFilter`, and `CharacterSummary`. Milestones 1 through 3 may implement that subset and Express-wide logging after execution authorization. After the owner updates the canonical task/specification allocation, revise Milestone 4 to name the approved detail fields and types. TASK-006 never exposes `Mutation`, mutation names, or placeholder mutation resolvers.
  Rationale: TASK-008 owns mutations, while the current records disagree about the bounded-comment part of the detail read. Publishing unallocated detail fields or unusable mutation fields would be misleading implementation evidence and would weaken schema/runtime alignment.
  Date/Author: 2026-08-16 / Codex primary coordinator.
- Decision: Do not allocate bounded comment reads unilaterally in this ExecPlan. Require the project owner to reconcile the canonical TASK-006/TASK-008 records and routed specifications before Milestone 4 and task closure, while allowing unrelated summary, filter, and logging work to continue after execution authorization.
  Rationale: TASK-006's detail outcome and ADR-0006 support a read-side allocation, while TASK-008 explicitly owns ordering/pagination. `PLANS.md` prohibits an ExecPlan from changing task scope, and ADR-0016 stops only dependent `CONFLICTING` work.
  Date/Author: 2026-08-16 / Codex primary coordinator.
- Decision: Supersede the registration-era global comment barrier and standalone dependency milestone with four behavioral milestones plus closure.
  Rationale: the earlier barrier serialized independent work, and the separate setup milestone produced no requirement or acceptance yield. The revised milestones preserve every worker-first invariant while aligning recurring coordination cost with observable product outcomes.
  Date/Author: 2026-08-16 / Codex primary coordinator.
- Decision: Implement server-side GraphQL resolver type generation in Milestone 1 and defer frontend client-operation generation to TASK-009.
  Rationale: ADR-0002 requires both categories to derive from the version-controlled schema, but TASK-006 owns only the server schema and resolver boundary. Generated artifacts are never hand-edited, and a deterministic generation/drift check joins typecheck and build.
  Date/Author: 2026-08-16 / Codex primary coordinator.
- Decision: Interpret FR-BE-006 literally as one bounded record for every completed Express request, not only GraphQL requests.
  Rationale: mandatory requirement wording outranks SPEC-013's narrower example. `/healthz` proves the non-GraphQL case while retaining its existing response and liveness-only meaning; safely unavailable GraphQL metadata is recorded as the DPL-defined null/absent value.
  Date/Author: 2026-08-16 / Codex primary coordinator.
- Decision: Reuse the existing `api-unit`, `api-application`, and serial `api-persistence-integration` Vitest projects rather than create another runner project.
  Rationale: Their current include patterns and environments already own the needed unit, in-process Express, and real-PostgreSQL boundaries. New runner configuration would add no isolation or evidentiary value.
  Date/Author: 2026-08-16 / Codex primary coordinator.
- Decision: Route Milestone 1 as one coordinator-confirmed `PARTIAL` cycle and require clean-checkout or hosted evidence at Milestone 1's first manifest/dependency/build boundary and Milestone 2's later production-composition/startup boundary.
  Rationale: ADR-0016 requires clean-environment proof at the first milestone that changes each distinct boundary. Combining dependency setup with the transport Green removes a zero-yield milestone without pretending that later startup evidence can reuse an earlier tree after production composition changes.
  Date/Author: 2026-08-16 / Codex primary coordinator.
- Decision: Group reversible choices into milestone-local DPL records: GraphQL transport/schema/type generation before Milestone 1, request-record shape before Milestone 3, and any owner-assigned comment tie-breaker before Milestone 4.
  Rationale: these choices are needed before dependent tests but do not require one durable record per key, unit, or rounding detail. Grouped decisions preserve traceability without letting the decision log dominate a small portfolio feature.
  Date/Author: 2026-08-16 / Codex primary coordinator.
- Decision: Do not change the root README merely to register this plan.
  Rationale: TASK-006 remains `Pending`, the repository still has no GraphQL product API, and `docs/plans/README.md` is the navigation owner for active plans. Root status changes when authoritative state or implementation evidence changes.
  Date/Author: 2026-08-16 / Codex primary coordinator.
- Decision: Activate TASK-006 from the owner's 2026-08-16 directive and keep Milestone 4's comment-read conflict as the only owner-controlled behavior barrier.
  Rationale: TASK-004 and TASK-017 remain `Complete`, DG-006 remains `Resolved`, AUTH-001 remains `Authorized` within its exact direct-URL scope, and the directive explicitly authorizes Milestones 1 through 3 while withholding the Milestone 4 allocation decision. Activation changes task/execution state only and does not create implementation evidence.
  Date/Author: 2026-08-16 / Codex primary coordinator.
- Decision: Adopt the exact Milestone 1 GraphQL transport, schema, resolver-type generation, endpoint, and explorer boundary recorded in DPL-DEC-040.
  Rationale: Yoga is the smallest current official direct Express 5 integration, its peer range fixes GraphQL 16.14.2, a pluckable checked-in TypeScript SDL literal avoids a runtime copy/loader, and direct Codegen plugins generate one resolver-signature file with a documented dry-run drift check without adding modular-preset or watch machinery.
  Date/Author: 2026-08-16 / Codex primary coordinator.
- Decision: Stop Milestone 1 before the single permitted Green correction rather than silently relax strict compiler policy, force `lru-cache` across Yoga's declared major-version range, or replace the recorded GraphQL runtime.
  Rationale: each route changes a packet or DPL binding beyond the accepted Green even though the observable test contract is unchanged. The owner-authorized workflow requires an explicit compatibility disposition at a binding-field stop; local adapter generic fixes alone cannot make the mandatory strict join pass.
  Date/Author: 2026-08-16 / Codex primary coordinator.
- Decision: Adopt DPL-DEC-041's `skipLibCheck: true` only in `apps/api/tsconfig.json`, retain all strict source checks, and track removal as a non-MVP-blocking engineering follow-up.
  Rationale: the no-write probe proves that the exception skips the incompatible third-party declarations while continuing to reject every local source typing defect; generated resolver types are ordinary `.ts` and remain checked. This is narrower and lower runtime risk than disabling a strict-family source check, forcing an undeclared transitive major, or replacing the selected GraphQL runtime. The owner explicitly authorized the exception and requested a post-MVP debt record, so the canonical plan and DPL now preserve its objective retirement check.
  Date/Author: 2026-08-16 / Project owner and Codex primary coordinator.
- Decision: Stop automatic continuation after the unsuccessful attempt-2 correction and retain its compliant partial diff for owner-directed disposition.
  Rationale: the owner-authorized workflow permits at most one same-contract correction per role and explicitly stops on exhausted budget. Read-only triage isolated an exact one-file type projection, but diagnosis is not authority to reset the worker budget or make an exceptional primary implementation edit.
  Date/Author: 2026-08-16 / Codex primary coordinator.
- Decision: Apply the owner-authorized exhausted-budget exception as one direct primary edit to `apps/api/src/transport/graphql/graphql-handler.ts` and preserve every other candidate boundary.
  Rationale: attempt 2 exhausted the code-worker correction budget after introducing an incorrect mandatory Express server context. Read-only compilation had already isolated the exact official direct-Express `{}` projection, so another worker cycle would add no discovery value. Recording the sole path, reason, and complete validation satisfies the repository's exceptional-edit rule without resetting the role budget or widening behavior.
  Date/Author: 2026-08-16 / Project owner and Codex primary coordinator.
- Decision: Add one exact-path `.gitattributes` classification for the canonical generated resolver file between leases.
  Rationale: hand-editing the generated terminal line violates deterministic drift, weakening whitespace checks globally hides unrelated defects, and changing the pinned generator or adding a formatter would widen the milestone. The exact `-blank-at-eof` attribute preserves canonical generated bytes and every other whitespace diagnostic. This is a durable generated-output classification, not new product debt; DPL-DEC-041 remains the explicit post-MVP compatibility debt.
  Date/Author: 2026-08-16 / Codex primary coordinator.
- Decision: Preserve failed commit `e68027b29d03144f6b69e57e6f04e5c7c99f1489` and extend only the same generated-path attribute with `text eol=lf`.
  Rationale: the required clean-checkout proof demonstrated that Git's Windows materialization, not source or generated identity, causes drift. Amending would erase point-in-time failure evidence; changing generator output, global line-ending policy, or drift semantics would widen scope. Exact-path LF materialization is the smallest reproducible correction and keeps every unrelated path on repository defaults.
  Date/Author: 2026-08-16 / Codex primary coordinator.
- Decision: Preserve follow-up commit `0659d1ee687c83aeb7fead9576ee60957544a78e` and add exact LF materialization for only the frozen Red test.
  Rationale: every behavior/build/drift gate passes and the committed test blob already has the accepted hash; only Windows working bytes differ. Rehashing normalized content would change the frozen-evidence convention, while a global LF rule would widen repository policy. One exact test-path attribute preserves the established byte contract across clean checkouts without changing test or product content.
  Date/Author: 2026-08-16 / Codex primary coordinator.
- Decision: Record but do not remediate the newly reported Sequelize/uuid advisories inside TASK-006.
  Rationale: Sequelize 6.37.7 and uuid 8.3.2 were established by completed TASK-004, TASK-006 did not add or change them, and no matching vulnerable operation is present in current source. Sequelize 6.37.8 addresses only the Sequelize advisory and still declares affected uuid 8.x; each advisory therefore needs an accurate compatible disposition plus rerun of the persistence lifecycle and affected GraphQL boundaries. Treat both as separate pre-production follow-ups, not as owner-approved post-MVP deferral or a reason to expand this milestone.
  Date/Author: 2026-08-16 / Codex primary coordinator.
- Decision: Add exact LF materialization only for the two frozen Milestone 2 Red tests before their authorized Windows clean-checkout proof.
  Rationale: both accepted filesystem hashes are LF identities and Git otherwise reports that Windows checkout conversion may rewrite them. Exact-path `text eol=lf` rules preserve the frozen contracts without changing either test, applying a glob, or widening repository line-ending policy.
  Date/Author: 2026-08-17 / Codex primary coordinator.
- Decision: Withhold Milestone 2 after the first S3 `REVISE` and request exact owner direction before a shutdown-race regression test or further product correction.
  Rationale: removing the prohibited default order is inside the existing product path, but executable proof of the newly found signal race requires a test path beyond the two exact frozen test-owner paths. That is a binding packet change, and the ordinary code-worker correction budget is already exhausted. The repository workflow therefore forbids automatic continuation even though the behavior remains within the Milestone 2 objective.
  Date/Author: 2026-08-17 / Codex primary coordinator.
- Decision: Offer one deterministic unit Red and one three-path exceptional Green as the smallest owner-authorization route for the S3 findings.
  Rationale: a real child-process/signal test would need platform-sensitive timing or a production-only test hook. An injected deferred initializer in `runtime-composition.unit.test.ts` can prove the exact adversarial ordering, while existing smoke lifecycle already proves built-process termination. Keeping the owner seam in `runtime-composition.ts`, wiring `server.ts` through it, and removing only the read repository's prohibited order closes both findings without a dependency, generic lifecycle framework, PostgreSQL fixture, or change to the two frozen tests.
  Date/Author: 2026-08-17 / Codex primary coordinator.
- Decision: Execute the exact owner-authorized Milestone 2 review-correction route without resetting any ordinary worker budget.
  Rationale: the owner explicitly authorized the proposed binding test path and exhausted-budget exceptional Green after receiving the exact files, behavior, and exclusions. The persistent test owner may create the single deterministic Red under an attempt-2 guarded lease; after coordinator acceptance, the implementation correction remains one exceptional owner-directed turn limited to the three named product paths. This authority does not permit a second test, a changed frozen test, dependency/configuration work, a generic lifecycle abstraction, or broader refactoring.
  Date/Author: 2026-08-17 / Project owner and Codex primary coordinator.
- Decision: Accept the deterministic shutdown-ownership Red and preserve its exact LF identity before exceptional Green.
  Rationale: strict compilation passed and the focused run failed solely on the absent owner seam, so the new scenario proves the classified missing gap without a timing, process, network, or infrastructure dependency. Its exact-path LF rule is necessary for the authorized Windows clean-checkout proof and changes neither test bytes nor repository-wide line-ending policy.
  Date/Author: 2026-08-17 / Codex primary coordinator.
- Decision: Adopt DPL-DEC-042's fixed bounded request-record shape before Milestone 3 Red.
  Rationale: the test-owner preflight confirmed that the behavior is absent but classified the slice `UNKNOWN` because no authority selected field names, bounds, duration semantics, or the null convention. One server-owned ID, Express path without query text, monotonic integer duration, safely observed GraphQL error count and named operation, fixed `operationName: null` fallback, per-field normalization and byte caps, and a 1024-byte LF-terminated JSON line resolve that reversible contract without a dependency, route, configuration surface, body retention, or generic logging framework.
  Date/Author: 2026-08-17 / Codex primary coordinator.
- Decision: Freeze the Milestone 3 Red only after one bounded test-owner correction adds a long valid GraphQL operation name.
  Rationale: the first coherent test correctly failed on zero records after exercising all three required HTTP paths, but its short names allowed a future unbounded-operation implementation to pass. Reusing the successful request with 160 safe ASCII bytes and requiring DPL-DEC-042's 96-byte projection closes the present S3 trigger without another request, test case, file, dependency, production seam, or expanded behavior. Keeping the three requests in one Vitest case is intentional because they jointly prove one exactly-once output sequence and share deterministic ID/time queues.
  Date/Author: 2026-08-17 / Codex primary coordinator.
- Decision: Withhold Milestone 3 Green at strict typecheck and authorize the ordinary same-contract Green correction on the unchanged four source paths.
  Rationale: focused and affected behavior pass, the lease is scope-compliant, and every diagnostic is confined to the new Yoga callable/plugin type projection. Installed local declarations already expose the required Node `.handle` overload and explicit plugin context generic, so correcting those annotations is implementation work within the frozen contract rather than a new declaration exception, dependency, test, path, or behavior decision.
  Date/Author: 2026-08-17 / Codex primary coordinator.
- Decision: Stop after the exhausted attempt-2 Green and offer one exact `app.ts` exceptional correction for owner authorization.
  Rationale: attempt 2 closed four of five diagnostics, and an in-memory strict compile isolates the last one to the adapter member plus exact-optional context construction. Changing the disk now would exceed the ordinary correction budget even though the proven replacement adds no behavior, path, dependency, configuration, or debt. Owner authority must explicitly permit the one-file exception before stale checks rerun.
  Date/Author: 2026-08-17 / Codex primary coordinator.
- Decision: Execute the owner-authorized exceptional `app.ts` adapter correction directly in the primary context.
  Rationale: the ordinary worker budget is exhausted, the exact two-part projection already compiles with zero diagnostics in memory, and another worker cycle would add no discovery value. Limiting the direct edit to the proven call/context construction and rerunning every stale check satisfies the repository's exceptional-edit rule without creating technical debt or changing behavior.
  Date/Author: 2026-08-17 / Project owner and Codex primary coordinator.
- Decision: Reject and restore the authorized method after focused behavior, then request revised owner direction for the narrower `.handle` exact-optional correction.
  Rationale: strict typing alone was insufficient evidence: `handleNodeRequestAndResponse` does not perform the Node send step and therefore hangs the Express request. The established generic `.handle` route already passed focused and affected behavior; overload probes prove only its undefined-valued optional context caused TS2769. Preserving `.handle` and conditionally omitting the property is smaller and zero-diagnostic, but it is a different explicitly named method and cannot silently reuse the prior authorization.
  Date/Author: 2026-08-17 / Codex primary coordinator.
- Decision: Apply the owner's standing authority to bounded TASK-006 corrections and use a severity-classified post-MVP debt disposition only for non-blocking, future-safe issues.
  Rationale: this removes repeated approval pauses for same-contract corrections while preserving the worker-first attempt limits, primary evidence ownership, mandatory gates, security and correctness stops, and external-publication boundary. A deferred issue must record `Critical`, `Medium`, or `Low`, its present impact, why deferral is safe, and an exit criterion; Critical items require case-specific owner review and no item that blocks mandatory behavior, security, correctness, or closure may be relabeled as debt. The revised `.handle` exact-optional edit is already bounded to `app.ts` and zero-diagnostic in a read-only strict compile, so it may now proceed directly.
  Date/Author: 2026-08-17 / Project owner and Codex primary coordinator.
- Decision: Correct the sole S3 finding with one exact frozen-test LF attribute under standing owner authority.
  Rationale: the review passed the entire product contract but proved that `core.autocrlf=true` can change the untracked frozen test's working bytes after a clean Windows checkout. Adding only `apps/api/src/transport/http/request-log.application.test.ts text eol=lf` changes no test or product byte, follows the established TASK-006 identity pattern, and is necessary for the promised hash to survive exact committed proof. Confirmation must verify effective attributes, zero CRLF pairs, the frozen hash, diff checking, and a Windows clean checkout before milestone acceptance.
  Date/Author: 2026-08-17 / Codex primary coordinator.


## Outcomes & Retrospective


Milestones 1, 2, and 3 are accepted from exact committed proofs `aa4454ceed074540d35e48945b1ba671ea24900c`, `563875cf76925a1a5e790b96d3721d699759c6b1`, and `14ae00b40ce915e784535a0e78e4cc7f1271e9e8`. Milestone 1 established the typed GraphQL summary boundary and preserves DPL-DEC-041 as explicit `Medium` post-MVP declaration-compatibility debt. Milestone 2 provides the PostgreSQL-backed five-filter list path, byte-exact stored image projection, demand-lazy production composition, database-independent liveness, and shutdown-safe ownership of initialization in flight. Milestone 3 provides one bounded record for every Express response reaching `finish`, safely selected GraphQL operation/error metadata, production stdout/UUID/monotonic wiring, and exact frozen LF identity. Its fresh S3 review passed the product contract, and the sole checkout-identity Major passed bounded correction confirmation and an exact Windows clean proof. Pre-existing Sequelize/uuid advisories remain separately scoped pre-production maintenance, not implicit post-MVP deferral. Milestone 4 remains unstarted and owner-blocked by the TASK-006/TASK-008 comment-read reconciliation. TASK-006 remains `In progress`, and no canonical AC or full NFR-003 claim advances.


## Context and Orientation


The source requirements are FR-BE-001 (GraphQL through Express), FR-BE-002 (status, species, gender, name, and origin filters), FR-BE-006 (middleware logs each request), the TASK-006 portion of NFR-003, and AC-007, AC-008, and AC-011. OR-001, OR-007, and OR-008 remain source-optional but are adopted repository commitments: implementation and tests use strict TypeScript, the character-search service receives unit coverage, GraphQL resolver types derive from the version-controlled schema, and transport, application, and persistence responsibilities remain separate.

ADR-0002 controls strict TypeScript. ADR-0003 controls PostgreSQL, explicit searchable columns, and literal safe filters. ADR-0005 is relevant supporting authority if the owner assigns bounded/newest-first comment reads to TASK-006, but it is not added to the canonical governing list before that reconciliation. ADR-0006 owns the project GraphQL operations, projections, input semantics, stable error codes, thin resolvers, service/repository direction, logging metadata, and development documentation. ADR-0011 owns unit, application, integration, and smoke boundaries. ADR-0014 requires exact stored absolute `imageUrl` projection and forbids image bytes or a proxy/asset route. ADR-0016 owns preflight classification, coherent milestone-slice TDD, independent test and implementation ownership, evidence reuse, milestone validation, and review routing.

The undisputed routed executable targets are the query portions of SPEC-008, all of SPEC-009 and SPEC-013, the TASK-006 GraphQL-boundary contribution to HS-007, the query-applicable portions of HS-008, and all of HS-010 and HS-014. Any TASK-006 contribution to HS-009 depends on the owner-controlled comment-read reconciliation. The Gherkin files remain non-executable documentation. Tests cite stable IDs through names, rationale, or an owned mapping; no Cucumber layer is added.

TASK-004 already created the `characters` and `comments` tables and `createSequelizePersistenceAdapter({ sequelize, schema })`. Its migration files, authenticated-artifact lifecycle, lock identity, and schema authority are prerequisites, not TASK-006 edit targets. Runtime queries operate only against a namespace already migrated by the documented migration command or a run-owned integration fixture. The API must never call `sequelize.sync()` or apply migrations during startup.

Committed HEAD now contains the accepted Milestone 1 boundary: exact DPL-DEC-040 manifest/lock entries, an injected conditional `/graphql` seam, checked-in query schema, generated resolver types, thin resolvers/handler, explicit application interfaces, exact LF identities, and DPL-DEC-041's API-local declaration exception while preserving `createApp()` as health-only composition when no GraphQL dependency is supplied. `apps/api/src/server.ts` still parses the loopback host/port and starts that health-only composition; production PostgreSQL composition remains Milestone 2. `vitest.config.ts` registers `api-unit`, `api-application`, and serial `api-persistence-integration`; root `test:integration` is active. `withPostgresNamespace` plus `prepareMigratedNamespace` provides run-owned real-PostgreSQL setup and cleanup.

In this plan, a summary is exactly `{ id, name, imageUrl, species }`. The final detail projection must follow the owner-reconciled TASK-006/TASK-008 allocation before its test contract is frozen; ADR-0006 includes status, gender, character `type`, origin, global `isFavorite`, and bounded comments. A normalized filter has blank values removed; status/gender are case-insensitive exact values; name/species/origin are case-insensitive literal substrings; supplied filters combine with `AND`. A stable GraphQL error is identified by `extensions.code`, not by an invented HTTP status. A request record is one bounded single-line JSON object on standard output for every completed Express request, distinct from server-side error diagnostics; GraphQL-only metadata is null or absent according to the pre-implementation DPL contract when unavailable.

Direct successors are TASK-007 (Redis cache-aside), TASK-008 (mutations), and TASK-009 (frontend GraphQL-client decision). TASK-005 may execute concurrently only in a separate worktree or under an explicit merge order because both tasks can touch the API manifest and root lockfile. DG-003 does not govern backend GraphQL implementation and must not introduce a frontend client, generated operation, or query cache here.


## Scope and Non-Goals


In scope:

- one version-controlled, build-safe query schema served through one Express GraphQL HTTP boundary;
- generated server-side resolver types derived deterministically from that schema, plus a generation/drift check integrated with the build boundary;
- one development-only explorer backed by that same schema and unavailable outside the recorded development boundary;
- explicit summary read models, thin transport resolvers, application read services, and a Sequelize/PostgreSQL business repository, plus only the detail read surface later assigned by the project owner;
- deterministic database fixtures independent of TASK-005 and the live public API;
- all five filters, whitespace/case normalization, blank omission, combined `AND`, no-match empty list, and literal `%`, `_`, quote, and combined metacharacter behavior using safely bound values;
- positive base-10 ID validation, `BAD_USER_INPUT`, query-side `NOT_FOUND`, and redacted `INTERNAL_SERVER_ERROR` behavior;
- exact stored `imageUrl` projection without revalidation, rewriting, fetching, proxying, or byte ownership;
- one bounded structured request record per completed Express request, including `/healthz` and GraphQL success/failure paths, and separate server diagnostics for unexpected errors;
- lazy production composition that keeps process startup and `/healthz` independent of PostgreSQL readiness; and
- API contract/operation examples, setup prerequisites, status, traceability, review, and task-closure documentation after behavior exists.

Out of scope:

- the TASK-005 importer, live upstream character JSON calls, scheduled synchronization, or import cache invalidation;
- Redis dependencies, cache keys, cache-aside behavior, or Redis failure policy, all owned by TASK-007;
- `Mutation`, favorite/comment mutation resolvers, comment input validation, mutation persistence, or mutation-driven readback, all owned by TASK-008;
- frontend GraphQL client selection, generated frontend operation types, CORS for the future browser product path, query cache, UI, or product browser smoke, owned by TASK-009 and TASK-010;
- database migrations, DDL, migration artifact/lifecycle/lock changes, `sequelize.sync()`, API-startup migrations, or a second persistence schema;
- image validation/import, image bytes, decoding, an image table, alternate host, asset/proxy route, browser image behavior, CSP, fallback UI, or content-rights changes;
- a parallel REST product API, readiness endpoint, microservice, event bus, or cross-process application boundary;
- authentication, sessions, users, authorization, anonymous-public deployment controls, or a claim that the mutation surface is production-ready;
- server-side A-Z/Z-A character sorting or a promised default list order; ADR-0006 leaves sorting with the frontend baseline, so filter tests compare results without inventing API ordering;
- Swagger (deferred OR-009), executable Gherkin, Storybook, numeric coverage thresholds, broad browser end-to-end tests, or a new CI provider; and
- a claim that full NFR-003, AC-004/005/010/012, overall product acceptance, imported baseline, frontend behavior, or final delivery passes.

Deterministic bounded comment reads, their valid and invalid pagination behavior, ordering, and the HS-009 read contribution remain unallocated in this plan. They cannot enter TASK-006 scope, tests, interfaces, commands, or worker leases until the project owner updates the canonical TASK-006/TASK-008 records and specification routing. The primary must then revise Milestone 4 and its dependent acceptance text before detail execution proceeds. This unresolved branch does not block Milestones 1 through 3.


## Plan of Work


All implementation uses the accepted [worker-first workflow](../../.codex/execplan-implementation-workflow.md) and [write-lease guard](../../.codex/write-lease-guard.md) only after TASK-006 becomes `In progress`. Each milestone has one read-only preflight, one coherent `evidence` or Red assignment when applicable, one Green with optional same-turn behavior-preserving Refactor, at most one same-contract correction per role, and one review correction loop. `test_worker` and `code_worker` remain separate; Red and Green are sequential; every write turn has a fresh packet and terminal lease; the primary inspects the actual diff and evidence. Two identical decisive failures, two no-diff write handoffs, a binding-field change, stale evidence, a noncompliant lease, or an exhausted budget stops automatic continuation.

Before each milestone is accepted, audit the affected tests, fixtures, helpers, mocks, snapshots, skipped/focused markers, and duplicated assertions for current semantic relevance, then run the focused Green, affected suites, and relevant type/build boundary once. Full registered scopes run once at closure unless a named invalidation or critical risk requires repetition. Real PostgreSQL evidence is normally non-reusable unless its exact run-owned namespace and state remain pinned. TASK-005 and TASK-006 writers never share this worktree concurrently; use a separate worktree or explicit merge order and repeat only invalidated checks after integration.


### Activation and localized authority barriers


Execution begins only after an explicit owner directive. The evidence-linked start row, TASK-006 `In progress` state, and current navigation must land as one coherent documentation change; no intermediate edit order is treated as valid authority. The primary then re-reads the current tree and DPL index, confirms DG-006/AUTH-001 continuity, and inspects TASK-005 manifest/lockfile activity.

Before Milestone 1, one grouped DPL record selects exact pinned GraphQL runtime/Express adapter and server-side code-generation packages, one endpoint, the checked-in schema representation, generated resolver-type output and drift command, and the development-only explorer boundary using current official Node 24/Express 5 compatibility evidence. Before Milestone 3, one grouped DPL record fixes the bounded request-record contract. Comment ordering is recorded only if the owner later allocates that behavior to TASK-006. Any choice that adds a second API/server, changes accepted architecture, exposes mutations, or changes the image boundary stops for the authoritative decision workflow.

The TASK-006/TASK-008 comment-read conflict is a localized Milestone 4 barrier. It does not block Milestones 1 through 3. Closure still waits for the owner-controlled reconciliation because TASK-006 cannot truthfully complete its detail outcome while that allocation remains contradictory.


### Milestone 1: Typed Express GraphQL summary vertical


Observable contract: an in-process Express application configured with an injected character-read service serves one checked-in query schema and `characters` operation, returns only `id`, `name`, `imageUrl`, and `species`, preserves an empty list, exposes no persistence-only field or `Mutation`, keeps the exact `/healthz` response, and exposes the same schema through a development-only explorer that is absent outside the recorded development boundary. Resolver types are deterministically generated from the schema and never hand-edited.

Preflight inspects the manifests/lockfile, Node/npm pins, Express 5, API TypeScript/build configuration, `app.ts`, existing health test, official adapter/code-generation compatibility evidence, and candidate transport/test paths. Classification is coordinator-confirmed `PARTIAL` only when `/healthz` is `EXISTING_AND_COVERED` and the typed GraphQL summary contract is `MISSING`; any incompatible evidence returns `CONFLICTING` or `UNKNOWN`. The Red reaches Express over HTTP and fails for the absent query contract, never for a missing compiler package, invalid test discovery, or changed health response.

The `test_worker` owns only `apps/api/src/transport/graphql/graphql-summary.application.test.ts`. The `code_worker` Green owns the exact root/API manifest and lockfile changes, schema/code-generation configuration and generated resolver types, the smallest schema/resolver/HTTP-handler paths, summary application interfaces, and `app.ts` injection seam. It must not add Sequelize queries, production database composition, filters, detail, request logging, mutations, client-operation generation, CORS, or image routes.

Risk is S2. Escalate to S3 for custom GraphQL-over-HTTP protocol code, production explorer exposure, raw body/secret exposure, handwritten duplicate resolver types, schema/type drift, mutation/image fields, or resolver-to-Sequelize access. Budget is the common single-cycle budget above. Focused proof is the summary application test; the join adds the affected application scope, unchanged health test, deterministic schema-generation/drift check, `npm run typecheck`, and `npm run build`. Because this milestone first changes manifests, dependency resolution, generated build inputs, and the build boundary, it also requires explicitly authorized clean-checkout or hosted evidence for this exact tree. A fresh `independent_reviewer` reviews the boundary. Acceptance yield is ADR-0002 server-type evidence, the TASK-006 contribution to HS-006/HS-007, and partial SPEC-008/HS-008; no AC completes before real PostgreSQL composition.


### Milestone 2: PostgreSQL character search and all five filters


Observable contract: production composition and a real migrated PostgreSQL namespace serve `characters(filter)` through Express, an application service, and a Sequelize-owned repository. Deterministic fixtures prove all five matching rules, blank omission, combined `AND`, no-match empty lists, literal `%`, `_`, quote and combined metacharacters, and byte-for-byte stored `imageUrl` projection. Unit evidence proves normalization/service coordination for adopted OR-007; integration evidence proves bound PostgreSQL semantics and the full HTTP path.

Preflight targets Milestone 1's accepted contract, `createSequelizePersistenceAdapter`, PostgreSQL lifecycle helpers, model tests, API/server/config composition, and existing query tests. TASK-004 migration/model behavior is reused without a fabricated Red. The aggregate route is `PARTIAL` only when the transport and persistence foundations pass and the PostgreSQL-backed search gap is explicit; otherwise stop on `CONFLICTING` or `UNKNOWN`.

The `test_worker` owns `apps/api/src/application/characters/character-filter.unit.test.ts` and `apps/api/src/transport/graphql/graphql-filters.integration.test.ts`. The `code_worker` owns minimum service/filter changes, `apps/api/src/infrastructure/database/sequelize-character-read-repository.ts`, a small lazy runtime composition boundary, and necessary `app.ts`, `server.ts`, or config changes. A required change to `sequelize-persistence.ts` stops for justification and an explicit TASK-004 regression expansion before a lease.

Risk is S3 because untrusted GraphQL input reaches SQL and production composition must preserve liveness isolation. Critical triggers include raw interpolation, input entering SQL structure, shared cleanup, live upstream access, eager database authentication, startup migration/import, `sync()`, migration/artifact/lock changes, Redis, or a task-external route. Budget is the common single-cycle budget. The focused command runs the unit and real-PostgreSQL filter files; the join adds affected API unit/application/integration scopes, `npm run typecheck`, `npm run build`, smoke, and smoke lifecycle. This is the first production-composition/startup change, so it requires separately authorized clean-checkout or hosted evidence for its exact tree; Milestone 1 build evidence cannot substitute after production paths change. A fresh `critical_reviewer` reviews SQL safety, isolation, cleanup, startup, URL projection, and boundaries. Acceptance yield is SPEC-009/HS-010 plus list portions of SPEC-008/HS-008 and AC-007/AC-008 evidence candidates.


### Milestone 3: Bounded metadata for every Express request


Observable contract: every completed request handled by the Express application writes exactly one parseable, bounded, single-line JSON record to standard output. Tests cover `/healthz`, GraphQL success, and GraphQL failure. DPL-DEC-042 fixes the ordered keys as `requestId`, `method`, `path`, `status`, `durationMs`, `errorCount`, and `operationName`; safely unavailable GraphQL operation metadata is always `null`. The server owns the ID, the path excludes its query string, time is monotonic whole milliseconds, error count comes only from safely observed GraphQL result metadata in this milestone, strings are normalized and clipped to their recorded byte limits, and the LF-terminated UTF-8 record cannot exceed 1024 bytes. It excludes headers, bodies, query strings, GraphQL documents and variables, comment text, authorization data, cookies, secrets, stacks, SQL/Redis detail, internal paths, and diagnostic content. `/healthz` keeps its exact response and liveness-only meaning.

Preflight targets Express middleware order, the accepted GraphQL adapter hooks/result visibility, current console/stdout use, and existing health/GraphQL application tests. It classifies health response behavior as `EXISTING_AND_COVERED` and the all-Express logging contract as the explicit missing or partial gap. The Red must fail for an absent, duplicate, unbounded, or unsafe record, not because a route or test server is invalid. This milestone depends on Milestone 1 but not on PostgreSQL or the comment-read reconciliation.

The `test_worker` owns `apps/api/src/transport/http/request-log.application.test.ts`. The `code_worker` owns `apps/api/src/transport/http/request-log-middleware.ts` and only necessary handler/app wiring. It uses injected output, ID, and time boundaries for deterministic tests; it may not parse or retain arbitrary body content, add a logging dependency without a recorded present need, or change GraphQL behavior.

Risk is S3 because logging can leak sensitive or unbounded content. Critical triggers include any captured header/body/secret/stack, nondeterministic unbounded operation metadata, missing or duplicate records, changed health semantics, or diagnostic-to-client/stdout leakage. Budget is the common single-cycle budget. Focused proof is the request-log application test; the join adds affected application/integration scopes, `npm run typecheck`, and `npm run build`. A fresh `critical_reviewer` inspects serialized success/failure/non-GraphQL records and negative fixtures. Acceptance yield is SPEC-013, the all-request and unsafe-data-exclusion portions of HS-014, FR-BE-006, and an AC-011 evidence candidate.


### Milestone 4: Owner-reconciled detail projection and query errors


This is the only behavior milestone blocked by the comment-read conflict. TASK-006 owns a character-detail outcome, while TASK-008 explicitly owns deterministic comment ordering/pagination and the routed records disagree about bounded reads. Until the project owner updates the authoritative task and specification owners, preflight returns `CONFLICTING`; no detail test path, production path, command, or lease is authorized. Milestones 1 through 3 remain independent.

After reconciliation, revise this milestone's exact schema fields, paths, focused command, yield, and comment-specific validation before preflight. Its coherent contract must cover `character(id)`, the approved detail projection, positive base-10 ID validation, `BAD_USER_INPUT`, valid missing-ID `NOT_FOUND`, unexpected-failure `INTERNAL_SERVER_ERROR`, and server/client diagnostic separation. If TASK-006 receives comments, the same contract includes valid bounds/order plus invalid-pagination errors. If TASK-008 retains them, the staged TASK-006 schema must omit unavailable comment behavior without weakening the owner-approved final contract.

The future `test_worker` owns the reconciled detail/error application and necessary real-PostgreSQL tests; the `code_worker` owns only the corresponding schema/codegen update, resolver/service/repository detail path, and typed error mapping. Generated types remain code-generated and frozen tests remain unchanged during Green. Risk is at least S2 and becomes S3 for comment-sensitive data, pagination/order integrity, unsafe error projection, auto-exposure, image transformation, or any write path. Budget remains one coherent cycle under the common limits. The join includes the focused Green, affected API unit/application/integration scopes, schema drift check, `npm run typecheck`, and `npm run build`, followed by the risk-routed fresh reviewer. Acceptance yield is the owner-approved detail/query-error portion of SPEC-008/HS-008, the unexpected-failure diagnostic-separation scenario in HS-014, and conditional HS-009 evidence.


### Milestone 5: Integrated validation, documentation, and closure


No new production behavior begins here. Perform the cumulative ADR-0016 semantic relevance audit over every TASK-006 test, fixture, helper, mock, snapshot, skipped/focused marker, and duplicated assertion; this joins rather than replaces the affected-test audit already completed at each milestone. Any newly discovered behavior gap returns to a new preflight-classified slice.

Run one authoritative closure packet from the repository root: strict typecheck, root `npm test` once, production build, Tailwind validation, smoke lifecycle, documentation validation, ADR validation when affected mappings remain in the diff, `git diff --check`, and explicit status/HEAD evidence. Do not repeat the unit/integration/application/smoke leaves merely to restate `npm test`.

Use the acceptance-review workflow for a dated TASK-006 review. A fresh integrated `independent_reviewer` concentrates on cross-milestone behavior, schema/type drift, traceability, dependency direction, lazy startup, SQL safety, error/log redaction, image absence, downstream boundaries, test relevance, clean-environment evidence, and documentation closure; route any remaining critical trigger to `critical_reviewer`. Only a closure-permitting verdict allows the primary to update API usage documentation, current status and evidence, TASK-006 state, affected indexes/specifications, and chronology. Change the authoritative task to `Complete` and move this plan to `docs/plans/completed/` only in the same closure reconciliation after every join passes.


## Concrete Steps


Run all commands from the repository root in PowerShell unless a future Milestone Assignment Packet says otherwise. The commands below already exist except the selected immutable dependency-install command, schema-generation/drift command, and future focused test paths; those become authoritative only after the activation DPL record and owning milestone create them. Do not treat a placeholder package or script name as executable evidence.

Registration checks:

    python .agents/skills/verify-repository/scripts/validate_docs.py --repo .
    python .agents/skills/govern-adrs/scripts/validate_adrs.py --repo .
    git diff --check
    git status --short

Milestone 1 pinned dependency installation, authorized only inside its Green write lease:

    npm install --workspace @rick-and-morty/api --save-exact graphql@16.14.2 graphql-yoga@5.21.2
    npm install --workspace @rick-and-morty/api --save-dev --save-exact @graphql-codegen/cli@7.2.0 @graphql-codegen/typescript@6.1.0 @graphql-codegen/typescript-resolvers@6.1.0

Closed local PostgreSQL profile and available milestone/closure commands:

    $env:POSTGRES_USER = 'rick_and_morty'
    $env:POSTGRES_PASSWORD = 'local-development-only'
    $env:POSTGRES_DB = 'rick_and_morty'
    $env:POSTGRES_SCHEMA = 'public'
    $env:POSTGRES_PORT = '5432'
    $env:POSTGRES_MIGRATION_LOCK_TIMEOUT_MS = '5000'
    $env:REDIS_PORT = '6379'
    npm run infra:ps
    npm run infra:up
    npm run infra:ps
    npm run typecheck
    npm run build
    npm run test:unit
    npm run test:application
    npm run test:integration
    npm run test:smoke
    npm run test:smoke:lifecycle

TASK-006 focused commands after the named files exist:

    npm run test:application -- apps/api/src/transport/graphql/graphql-summary.application.test.ts
    npm exec -- vitest run --config vitest.config.ts --project api-unit --project api-persistence-integration apps/api/src/application/characters/character-filter.unit.test.ts apps/api/src/transport/graphql/graphql-filters.integration.test.ts
    npm run test:application -- apps/api/src/transport/http/request-log.application.test.ts

Milestone 1 also runs the DPL-recorded deterministic server-schema generation and drift check after those exact scripts exist. Milestone 4's detail/error focused command is intentionally absent until owner reconciliation defines its contract and the required living-plan revision records the real paths and command.

The values above reproduce the repository's closed loopback-only local profile from `.env.example`; use an explicit free loopback port in the same shell if `5432` is occupied. Before `infra:up`, inspect `npm run infra:ps` and active task ownership. Reuse an already healthy exact profile without claiming ownership, or start the named `rick-and-morty-dev` project for this task. Run `npm run infra:down` only if this execution started that exact project, no other task uses it, and its owned namespaces have already cleaned up. Never tear down peer-owned infrastructure. Redis starts only because the current scoped Compose profile owns both services; TASK-006 never connects to it.

The decisive Red and matching Green use the same focused command within each milestone. A Red exits nonzero only for the accepted missing/regressed contract. Green exits zero with the same selected tests. PostgreSQL commands must report the run-owned namespace and cleanup result without exposing credentials; a primary failure and cleanup failure remain distinguishable. The owner-reconciled detail command is intentionally absent until the required plan revision defines it.

Closure packet, once after all milestone evidence is integrated:

    npm run typecheck
    npm test
    npm run build
    npm run validate:tailwind
    npm run test:smoke:lifecycle
    python .agents/skills/verify-repository/scripts/validate_docs.py --repo .
    python .agents/skills/govern-adrs/scripts/validate_adrs.py --repo .
    git diff --check
    git rev-parse HEAD
    git status --short

Expected closure results are zero exits, no skipped/focused tests, no run-owned database or process residue, a task-scoped review verdict that permits closure, and documentation whose task status, plan lifecycle, current evidence, and acceptance statements agree. Root `npm test` includes unit, integration, application, and smoke in canonical order; do not run those four aggregate leaves again at closure unless evidence invalidation is recorded.

Use targeted negative inspection in addition to semantic review. Exact patterns may be refined when paths exist, but the executor must prove:

- no `type Mutation`, `setCharacterFavorite`, or `addCharacterComment` in the TASK-006 schema/runtime;
- no handwritten duplicate resolver-type surface, stale generated output, generated-file hand edit, frontend GraphQL client/cache/operation-generation artifact, Redis client/cache behavior, or live Rick and Morty character-data request;
- no `sequelize.sync()`, API-startup migration/import, mutation of migration lifecycle files, or alternate schema authority;
- no image table/bytes/decoder/asset/proxy route or transformation of stored `imageUrl`;
- no parallel REST product route beyond the preserved `/healthz` and the one recorded GraphQL boundary;
- exactly one bounded request record for representative `/healthz`, GraphQL success, and GraphQL failure requests, with no raw body, variables, authorization, cookie, secret, comment text, stack, SQL detail, Redis detail, internal path, or diagnostic content in stdout or the client error; and
- no changes to `.gitignore`, Git index, HEAD, branch, or unleased paths during a worker lease.


## Validation and Acceptance


TASK-006 is accepted only when repository evidence proves all of the following:

- preflight classifications exist for every intended scenario, with no manufactured Red for `/healthz`, TASK-004 migration/model behavior, or an already-absent image route;
- each missing/regressed/confirmed-partial milestone has one accepted coherent Red, frozen test boundary, matching Green, affected-test relevance audit, affected-suite/type/build join, terminal compliant leases, primary diff inspection, and risk-routed semantic review;
- one Express GraphQL endpoint serves a version-controlled query schema, server-side resolver types are generated deterministically from it without hand edits or drift, and no mutation or parallel REST product contract is advertised;
- `characters` and the owner-reconciled `character` surface use thin resolvers, application read services, and a Sequelize/PostgreSQL repository, and the production runtime remains one process;
- summaries and the owner-reconciled detail projection expose only approved fields and exact stored absolute `imageUrl` values;
- status/gender use case-insensitive exact matching; name/species/origin use case-insensitive literal substring matching; case/whitespace normalization, blank omission, combined `AND`, no-match, and metacharacter cases pass with bound values at real PostgreSQL;
- the owner-reconciled detail contract passes exactly the fields, bounds, errors, and downstream allocation recorded in the updated canonical tasks and this revised plan; if TASK-006 owns comment pagination, valid bounds and typed invalid-pagination `BAD_USER_INPUT` evidence are both mandatory;
- invalid IDs never reach the service, missing valid IDs use `NOT_FOUND`, unexpected failures use redacted `INTERNAL_SERVER_ERROR`, and original diagnostics remain server-side;
- every completed Express request emits exactly one bounded metadata record with the accepted fields and no unsafe content; executable evidence includes `/healthz`, GraphQL success, and GraphQL failure;
- API startup and `/healthz` pass without PostgreSQL readiness, migration, import, Redis, or external network access;
- TASK-003 health/smoke, TASK-004 persistence/migration regressions, strict TypeScript, native build, and run-owned cleanup remain passing;
- the first manifest/dependency/build boundary and the later production-startup boundary each have clean-checkout or hosted-platform evidence for their exact trees under explicit authorization;
- milestone-local and cumulative semantic relevance audits find no skipped, focused, duplicate-without-confidence, implementation-detail-only, abandoned, or unmapped tests;
- query portions of SPEC-008, SPEC-009, SPEC-013, applicable HS-006, HS-007, HS-008, HS-010, and HS-014 map to ordinary executable tests or deterministic build checks and passing authoritative commands; any HS-009 contribution appears only if the project owner canonically assigns it to TASK-006; and
- the final review and documentation gate pass before canonical `Complete` state or any readiness change is claimed.

The task may contribute evidence for AC-007, AC-008, and AC-011. Whether those criteria change readiness status is decided from the final acceptance review and current repository evidence, not from this plan. NFR-003 remains partial until Redis and the remaining backend tasks exist. Mutation scenarios in SPEC-008 and HS-008, full HS-009, AC-004/005, caching, import, frontend behavior, final delivery, and overall product acceptance remain outside this task.


## Idempotence and Recovery


Plan registration and documentation validation are safe to repeat. DPL IDs are never reserved in advance or reused; re-list the table immediately before allocation. Dependency installation uses the repository's exact-package and lockfile policy. If install, schema generation, drift checking, or build fails, preserve the manifest/lock/generated diff and command evidence, close the lease, and triage; never hand-edit generated lockfile or resolver-type output, widen version ranges, or accept stale generated types merely to continue.

Every integration test allocates and drops only its exact run-owned PostgreSQL namespace in `finally`. Never point tests at the development `public` schema, never use broad database deletion, and never run `infra:down` while another task owns infrastructure. A failed cleanup is separately reported and blocks evidence acceptance; recovery identifies and removes only the exact owned resource after coordinator authorization.

An unexpected path, concurrent change, stale fingerprint, guard violation, wrong Red, review finding, or command failure stops writers. Terminally close the active lease, preserve user/peer work, inspect the actual tree and receipt, and issue a fresh packet only if the original authority, objective, scope, and expected result remain unchanged. Never recover with `git reset --hard`, `git checkout --`, broad deletion, rebaseline of a live lease, a worker-invoked guard, or concurrent Red/Green.

Generated `node_modules/`, `dist/`, browser/test output, and lease records are ignored side effects and not write-lease evidence. Use current repository commands for ordinary rebuild/cleanup; do not add ignore or attribute rules during a worker lease. If a missing legitimate exact-path rule is demonstrated, the primary makes and records an exceptional coordinator edit between leases after validating the exact path.

If TASK-005 changes `apps/api/package.json`, `package-lock.json`, shared runtime/configuration, or database read models in another worktree, merge at a named milestone barrier, re-run preflight for affected scenarios, update fingerprints, and repeat only invalidated checks. Do not overwrite or revert the other task. A still-unresolved comment allocation pauses only Milestone 4; if AUTH-001 reopens, pause only image-specific assertions and route its authoritative process while other work follows the current implementation-plan permissions.

The 2026-08-16 execution authorization permits primary-coordinator local commits and task-owned temporary clean-checkout worktrees for the explicitly required Milestone 1 and Milestone 2 exact-tree proofs. Push, pull request, hosted CI, deployment, and other external publication remain unauthorized. Preserve point-in-time commits, runs, failures, and reviews in chronology rather than rewriting them after a correction.


## Artifacts and Notes


Registration baseline:

- Branch: `agent/milestone-slice-tdd-workflow`.
- HEAD: `42d53699e9ceb5430576345d3fc3e81fe8a964aa`.
- Initial tree: clean.
- Canonical TASK-006 state: `Pending`.
- Completed prerequisites: TASK-004 and TASK-017.
- Gate/authorization: DG-006 `Resolved`; AUTH-001 `Authorized` only within its recorded personal, educational, non-commercial direct-URL scope.
- Existing harness: `api-unit`, `api-application`, `api-persistence-integration`, root Chromium smoke.
- Existing API behavior: exact `GET /healthz` only.
- Existing database behavior: migrated character/comment schema and Sequelize model mappings; no business read repository.
- Missing behavior: GraphQL runtime/schema/route, schema-derived resolver types, list/detail services, filters, stable product errors, Express-wide request records, runtime database composition, and product tests.

Activation baseline:

- Authorization date: 2026-08-16.
- Branch: `agent/task-006-graphql-reads`.
- HEAD before activation edits: `21f2b5c0dc0fb5f4b11e2bf80da8810c5cbbb8a2`.
- Initial tree: clean; no peer worktree or TASK-005 branch was present.
- Canonical TASK-006 state after the coherent activation change: `In progress`.
- Infrastructure observation: `npm run infra:ps` could not reach a Docker engine, so no TASK-006 infrastructure ownership was claimed and Milestone 2 PostgreSQL evidence remains non-reusable until a run-owned identity exists.

Milestone 1 stopping-point identity:

- Workflow/cycle: `TASK-006-20260816-01` / `TASK-006-M1-TYPED-SUMMARY`.
- Accepted Red lease: `TASK-006-20260816-01-M1-red-02`; exit 1 with five intended absent-`/graphql` failures.
- Green lease: `TASK-006-20260816-01-M1-green-01`; terminally compliant with the frozen test unchanged.
- Passing evidence: focused application 5/5, affected application 6/6 including unchanged health, and `graphql:check`.
- Blocking evidence: root `npm run typecheck` exit 1 on local Yoga/Express typings and transitive `lru-cache` 10.4.3 declarations under TypeScript 6.
- Compatibility probes: API `tsc` with `--skipLibCheck` and with `--strictBuiltinIteratorReturn false` each report only the same five correctable local errors; neither probe edits the repository.
- Authorized disposition: DPL-DEC-041 permits API-local `skipLibCheck: true`, freezes every other compiler/product/dependency boundary, and records deletion plus passing root typecheck/build/affected tests as the post-MVP debt exit.
- Attempt-2 correction: lease `TASK-006-20260816-01-M1-green-02` closed compliant; API typecheck exits 1 on two server-context errors; a no-write in-memory probe confirms the exact `{}` server-context projection compiles with zero diagnostics.
- Exceptional correction: the owner authorized one primary edit to `graphql-handler.ts`; only the failed Express context declarations were removed and the first Yoga generic became `{}`. API/root typecheck, focused 5/5, drift, affected 6/6, root build, and audits pass.
- Review: fresh S2 verdict `PASS WITH FOLLOW-UPS` on relevant-tree fingerprint `E8AAA3FE8E24D4AD94A6F7D18543C8B3510D2D1059053D0C918CDF0DC06F9D4D`; no Blocker or Major; the sole documentation Minor was closed by bounded `PASS` confirmation with no finding.
- Generated-output integration: canonical generated SHA-256 remains `69F00BC096546F885E9CDEE15D5F41E7B470BECBC97E08EE191F1F22CA078628`; `.gitattributes` disables only `blank-at-eof` for that exact path, and cached/working-tree diff checks exit 0.
- Generated-output review: bounded correction-loop verdict `PASS` with no finding on relevant-tree fingerprint `ED479BE13F96B3B4952242EAA15415FCDFD4198D78FAD97C96DA81CBB26E0753`; no repository change.
- First committed candidate: `e68027b29d03144f6b69e57e6f04e5c7c99f1489`; primary worktree clean after commit.
- Failed clean proof: detached path `C:\Users\mmjos\AppData\Local\Temp\rick-and-morty-task006-m1-e68027b`; exact install/direct pins/focused 5/5/affected 6/6/typecheck/docs pass; `graphql:check` fails on CRLF materialization; build not run.
- Correction: the same exact `.gitattributes` rule now also sets `text eol=lf`; local attribute, drift, and diff checks pass.
- Correction review: LF semantics passed; advisory evidence returned `REVISE` on fingerprint `485B3AC1016103E62A8694F5CE27233A93C66150C91263D4D2D66DBDE14F8F90`. The living plan and append-only chronology now distinguish the Sequelize 6.37.8 fix from the still-open compatible uuid remediation investigation.
- Correction confirmation: `PASS` with no Blocker, Major, or Minor; exact three-path staged scope and no unstaged change; no reviewer edit.
- Reviewed follow-up: commit `0659d1ee687c83aeb7fead9576ee60957544a78e`; generated LF and every executable/static gate pass in detached clean checkout.
- Frozen-test stop: clean working SHA-256 `83A6EFED6B697CE0EAA803B4890C37B125FCA41E97490ACFF2B9DFBF24D30ECC`; exact committed blob SHA-256 `B783298F44BE7C104A498372746D69D5905254CB63C453B4CDAACF049FCAAC07`; no acceptance.
- Frozen-test correction review: `PASS` with no finding on milestone relevant-tree fingerprint `858CAB7C95169145DEC521A7FD5F1FFB0B8031549FB75BC1A1CE62FE08384213`; both paths `i/lf w/lf`, hashes exact, no reviewer edit.
- Accepted commit: `aa4454ceed074540d35e48945b1ba671ea24900c`.
- Passing clean proof: `C:\Users\mmjos\AppData\Local\Temp\rick-and-morty-task006-m1-aa4454c`; Node 24.18.0/npm 11.16.0/Windows `core.autocrlf=true`; every Milestone 1 gate and both frozen hashes pass with clean status.
- Tree state: primary worktree clean at the accepted product commit before this evidence reconciliation; Milestone 1 accepted, Milestone 2 unstarted.

Milestone 2 accepted identity:

- Workflow/cycle: `TASK-006-20260816-02` / `TASK-006-M2-POSTGRES-FILTERS`.
- Accepted Red lease: `TASK-006-20260816-02-M2-red-01`; frozen filter unit/integration SHA-256 values `04C0639B916061B28FE8295CF51BE64E289BF9971D7EF700FD14B29A57B3B41E` and `69F8E334D51D65F22872C98A8A72C0059E801E50BCA2ADB9C4C5F68D63B9CA61`.
- Review-correction Red lease: `TASK-006-20260816-02-M2-red-02-scope-only`; frozen shutdown-owner SHA-256 `CDAC325DCDC0361BE7F0C4736EB8D3F513692DCF4A0E6C2B5AD9A1A239BBBF42`.
- Terminal Green leases: attempt 1 receipt `9481100869e854ab6d583312454a7ff82a8357fbe11f512abc31b8d8cb00c42d`; attempt 2 receipt `1b340562ea59ed970512b248cfa1af1f0f56d30e2a7557c2498a211cb0192195`; owner-directed exceptional receipt `8e7d499e42c36e95be29d9c5f8dfa796bb883fc4a7f722e6a823b79d0f97d8e1`.
- Review: S3 product correction closed both prior Majors; one stale current-state Minor was corrected and bounded confirmation returned `PASS` with no remaining finding.
- Accepted commit: `563875cf76925a1a5e790b96d3721d699759c6b1`.
- Passing clean proof: `C:\Users\mmjos\AppData\Local\Temp\rick-and-morty-task006-m2-563875c`; Node 24.18.0/npm 11.16.0/Windows `core.autocrlf=true`; exact install and pins, all focused/affected/integration/build/smoke/lifecycle/documentation/static gates, all frozen LF identities, zero database residue/listeners, exact commit, and clean status pass. The first API-unit invocation shared a Vite temporary cache with concurrent build/application commands and failed one unrelated migration-artifact test on `EPERM` rename; the identical isolated rerun passed 57/57 without a tree change.
- Tree state: primary worktree clean at the accepted product commit before this acceptance reconciliation; Milestone 2 accepted, Milestones 3 and 4 unstarted.

During execution, retain only the concise identities needed to resume or validate work: workflow/milestone and terminal lease IDs, exact decisive commands/results, isolated PostgreSQL identity when applicable, milestone validation and reviewer verdict, clean-environment identity, and documentation-impact result. Detailed packets, routine hashes/fingerprints, secrets, full logs, generated guard state, and duplicated authority prose remain in their owning evidence rather than this living plan.

Expected production path families, subject to the accepted DPL naming record and preflight:

- `apps/api/src/application/characters/` for explicit read models, filter normalization, service, and repository port;
- `apps/api/src/infrastructure/database/sequelize-character-read-repository.ts` for PostgreSQL reads;
- `apps/api/src/transport/graphql/` for the checked-in schema, code-generation configuration/output, thin resolvers, and HTTP adapter;
- `apps/api/src/transport/http/request-log-middleware.ts` for bounded request metadata;
- a small API runtime composition module plus the existing `app.ts`, `server.ts`, and config boundary; and
- task-owned `.unit.test.ts`, `.application.test.ts`, and `.integration.test.ts` files selected by existing Vitest projects.


## Interfaces and Dependencies


DPL-DEC-040 pins runtime dependencies `graphql-yoga` 5.21.2 and `graphql` 16.14.2 and API development dependencies `@graphql-codegen/cli` 7.2.0, `@graphql-codegen/typescript` 6.1.0, and `@graphql-codegen/typescript-resolvers` 6.1.0. Yoga mounts directly on the existing Express 5 process at `/graphql` and uses `createSchema`; no separate Express adapter, JSON body parser, CORS middleware, or second server is added. One checked-in `apps/api/src/transport/graphql/schema.ts` `/* GraphQL */` SDL literal is the schema authority consumed by runtime and Codegen. API-local `graphql:generate` writes `apps/api/src/transport/graphql/generated/resolver-types.ts`; `graphql:check` uses Codegen `--check`, and the API build runs that drift check before TypeScript emit. Generated output is never hand-edited. Yoga's built-in GraphiQL is enabled only when an explicit development-composition boolean is true and is disabled by default, in tests, and in production. Frontend operation generation remains TASK-009; no client, cache, watch package, server preset, logging dependency, custom loader, or other schema framework is authorized.

DPL-DEC-041 adds `skipLibCheck: true` only to `apps/api/tsconfig.json` as a declaration-file compatibility exception. `strict: true` and every local-source check remain active; application source, tests, and generated resolver types remain checked `.ts`. The flag is explicit post-MVP technical debt, not a reason to force an unsupported transitive major: reassess after MVP or a supported compiler/Yoga dependency update, and remove it only when root typecheck, build, and affected TASK-006 tests pass without it.

The only schema target frozen before owner reconciliation is the undisputed query subset:

    type Query {
      characters(filter: CharacterFilter): [CharacterSummary!]!
    }

    input CharacterFilter {
      status: String
      species: String
      gender: String
      name: String
      origin: String
    }

`CharacterSummary` retains ADR-0006's exact fields and nullability. Generated resolver types derive from this schema and type only the transport boundary; application DTOs and persistence records remain explicit handwritten domain-facing types. ADR-0006 also targets `character`, `CharacterDetail`, `Origin`, and `Comment`, but this plan freezes no TASK-006 binding for those types until the owner reconciles the bounded-comment allocation and the primary revises this section. TASK-006 does not define `Mutation`; TASK-008 adds mutation behavior through its own test-first change.

The application boundary should remain no broader than:

    interface CharacterReadService {
      list(filter: CharacterFilterInput | undefined): Promise<readonly CharacterSummary[]>;
    }

    interface CharacterReadRepository {
      list(filter: NormalizedCharacterFilter): Promise<readonly CharacterSummaryRecord[]>;
    }

Transport input types remain distinct from normalized application types and persistence records. Resolvers validate/map and call the service; services normalize/coordinate; the repository alone owns Sequelize access. Summary DTOs explicitly select fields; no generic model serialization crosses the transport boundary. The post-reconciliation revision must add only the owner-approved detail method, input, DTO, persistence record, and projection.

Runtime composition reuses the DPL-DEC-025 closed PostgreSQL environment surface and loopback/no-TLS profile. It constructs a caller-owned Sequelize connection/pool lazily, selects the recorded schema, creates the read repository/service, injects them into the Express application, and closes only its owned runtime resource during controlled shutdown. It never calls `authenticate()`, `sync()`, migration code, importer code, or Redis as a prerequisite for process startup or `/healthz`.

The request-observability boundary wraps the Express application and accepts injected monotonic time, server-owned request-ID, and complete-line metadata-output boundaries for deterministic tests. Production writes one DPL-DEC-042 JSON-plus-LF record for every response that reaches Express `finish`. `/healthz`, non-GraphQL requests, anonymous operations, and requests without safely available GraphQL metadata use `operationName: null`; GraphQL success/failure adds only the bounded selected operation name and result error count. Original unexpected errors stay in the separate server diagnostic boundary owned by the detail/error contract. The implementation does not invent domain-specific HTTP statuses.

The exact stored `characters.image_url` value is returned byte-for-byte as the GraphQL string. TASK-006 neither re-derives the official URL nor validates/fetches it. Integration fixtures use deterministic recorded absolute URLs and fail closed on any unexpected live external request.

No comment-read interface, bound, ordering rule, error contract, or downstream consumer is authorized here. The owner-controlled canonical reconciliation determines those owners before Milestone 4 can define or implement them; Milestones 1 through 3 do not depend on that allocation.


## Revision Note


2026-08-16 / Codex primary coordinator: Created and registered the initial TASK-006 implementation ExecPlan from the post-TASK-004 repository. The plan preserves `Pending` state, reconciles adopted-optional traceability, surfaces the TASK-006/TASK-008 bounded-comment ambiguity for owner-controlled canonical reconciliation before any behavior Red, stages only the undisputed query schema before that reconciliation, reuses the existing Vitest/PostgreSQL harness, protects liveness from database readiness, and defines bounded worker-first milestones with ADR-0016 preflight and risk routing. It requires clean-checkout or hosted evidence at both the first dependency/build boundary and the later production-startup boundary. No task scope, product code, dependency, executable test, task start, gate status, acceptance result, or external state changed.

2026-08-16 / Codex primary coordinator: Revised the registration candidate after independent review. The revision removed all authoritative TASK-006 assumptions about bounded-comment ownership, blocked every behavior Red on owner-controlled canonical reconciliation, required the post-decision plan rewrite to own valid and invalid pagination together when applicable, made Milestone 2 one executable aggregate `PARTIAL` route, moved clean-environment proof to the first setup boundary while retaining a later startup-boundary proof, and added exact local PostgreSQL prerequisites plus ownership-safe cleanup. Registration validation passed, and fresh independent final-state review returned `PASS` with no Blocker, Major, or Minor on substantive plan SHA-256 `6847F6668139E7E2764F9BBBA7780A9E62A5E894B8C09A6816AD45625E6F9E8E`.

2026-08-16 / Project owner and Codex primary coordinator: Materially revised the living plan for portfolio proportionality without weakening ADR-0016 or the worker-first workflow. The revision supersedes the former global comment barrier and zero-yield dependency milestone, permits summary/filter/logging work after execution authorization, requires schema-derived server resolver types, restores FR-BE-006 to every Express request, combines detail with its query-error boundary, adds an affected-test audit to every milestone, and retains separate clean-environment proof for the distinct build and production-startup boundaries. TASK-006 remains `Pending`; no task scope, comment ownership, package, source, executable test, runtime evidence, gate/authorization status, acceptance result, or external state changed.

2026-08-16 / Project owner and Codex primary coordinator: Activated the owner-authorized ExecPlan after re-inspecting the clean worktree, branch, HEAD, manifests, current Express liveness source and coverage, infrastructure visibility, and concurrent TASK-005 state. TASK-004 and TASK-017 remain `Complete`, DG-006 remains `Resolved`, and AUTH-001 remains `Authorized` only within its recorded portfolio/direct-URL boundary. The coherent activation change moves TASK-006 to `In progress`, synchronizes current status, navigation, chronology, and all living sections, and adds no GraphQL dependency, schema, route, test, production behavior, runtime evidence, acceptance result, or Milestone 4 allocation.

2026-08-16 / Codex primary coordinator: Completed the required pre-Milestone-1 compatibility review against current official Yoga, Apollo Server, GraphQL Code Generator, and npm registry metadata; re-listed DPL-DEC-001 through DPL-DEC-039 immediately before allocating DPL-DEC-040. The grouped decision pins direct Yoga/Express integration, compatible GraphQL 16, a checked-in TypeScript SDL literal, direct resolver-type plugins with dry-run drift checking, `/graphql`, and explicitly development-only GraphiQL. It rejects GraphQL 17 as outside Yoga's peer range and adds no dependency or implementation artifact before the guarded Green.

2026-08-16 / Project owner and Codex primary coordinator: After the guarded Milestone 1 candidate stopped at strict typecheck, the owner explicitly authorized DPL-DEC-041's API-local declaration-file exception, one bounded same-contract Green correction, and a post-MVP technical-debt record. The decision keeps `strict: true`, tests local/application/generated `.ts`, rejects an out-of-range transitive override and strict-family source weakening, limits the correction to three paths, and defines removal as deleting the flag while root typecheck, build, and affected TASK-006 tests pass. No correction, review, commit, clean-checkout proof, milestone acceptance, or downstream behavior evidence is claimed by this documentation change.

2026-08-16 / Codex primary coordinator: The authorized attempt-2 Green correction closed compliant but stopped after API typecheck reduced five local errors to two related Yoga server-context mismatches. The worker correctly did not rerun now-stale behavior/drift evidence or broaden scope. Read-only in-memory compilation then confirmed the official direct-Express projection `createYoga<{}, GraphqlContext>` with zero diagnostics under the exact strict settings. The bounded correction budget is exhausted, so the primary preserved the partial diff and stopped for owner direction rather than issuing attempt 3 or making an unapproved direct implementation edit.

2026-08-16 / Project owner and Codex primary coordinator: The owner responded `go ahead`, explicitly authorizing the exhausted-budget exception. With no active lease, the primary edited only `apps/api/src/transport/graphql/graphql-handler.ts`, removed the failed Express server-context declarations, and selected `createYoga<{}, GraphqlContext>` exactly as the no-write probe established. API/root typecheck, focused 5/5, schema drift, affected 6/6, root build, exact pins, frozen hashes, relevance/marker audits, and diff checking pass. Fresh S2 review found no product defect and returned `PASS WITH FOLLOW-UPS` only because this authorization, reason, path, and evidence were not yet recorded; this revision and append-only chronology disposition that Minor without changing product behavior.

2026-08-16 / Codex primary coordinator: The pre-commit cached diff exposed a canonical GraphQL Code Generator terminal blank line that was invisible while the generated file was untracked. A deliberate one-line removal made the required drift check fail, proving the line belongs to generated identity. Between leases, the coordinator regenerated the unchanged artifact and added only `.gitattributes` with an exact-path `-blank-at-eof` classification. All other whitespace checks remain active, cached and working-tree diff checks pass, and no product or generated byte changed. This integration-only classification creates no additional post-MVP debt; the DPL-DEC-041 declaration exception remains the recorded compatibility follow-up.

2026-08-16 / Codex primary coordinator: Bounded correction-loop review passed the exact generated-output attribute with no finding. The reviewer independently confirmed exact-path scope, preserved default and neighboring whitespace behavior, canonical generated bytes and hash, passing drift/diff gates, and accurate living evidence on relevant-tree fingerprint `ED479BE13F96B3B4952242EAA15415FCDFD4198D78FAD97C96DA81CBB26E0753`. The reviewer made no repository change; the primary exact-tree commit and clean-checkout proof remain.

2026-08-16 / Codex primary coordinator: Created exact reviewed commit `e68027b29d03144f6b69e57e6f04e5c7c99f1489`, then preserved its failed authorized clean-checkout proof. The exact lockfile, pins, focused and affected tests, root typecheck, and documentation validator passed, but Codegen drift failed because Windows `core.autocrlf=true` produced CRLF working bytes in a Git-clean checkout. Root build and milestone acceptance were not claimed. The same exact generated-path attribute now also sets `text eol=lf`; local attribute inspection and drift pass without changing generated identity. The install additionally surfaced pre-existing Sequelize/uuid advisories; TASK-006 does not expand into their upgrade, and the evidence records separately authorized pre-production maintenance rather than treating security remediation as optional post-MVP debt.

2026-08-16 / Codex primary coordinator: Bounded review passed the LF correction itself but returned `REVISE` because the first advisory note incorrectly said Sequelize 6.37.8 remediates both findings. The append-only chronology now explicitly corrects that history, and the living evidence states the supported facts: Sequelize 6.37.8 fixes only its own advisory and still depends on affected uuid 8.x; uuid needs a separate compatible pre-production investigation. Current absence of matching vulnerable calls is reachability evidence only. No product, generated, dependency, schema, or test byte changed.

2026-08-16 / Codex primary coordinator: Bounded confirmation passed the corrected LF/advisory packet with no remaining finding. The reviewer confirmed exact staged scope, append-only correction, accurate distinct advisory dispositions, no security deferral claim, and passing evidence without changing the repository. The preserved follow-up commit and complete clean-checkout rerun are next.

2026-08-16 / Codex primary coordinator: Created preserved follow-up commit `0659d1ee687c83aeb7fead9576ee60957544a78e` and reran the full authorized clean proof. Generated LF identity and every install, dependency, drift, focused, affected, type, documentation, ADR, build, diff, marker, commit, and clean-status gate passed. The final byte audit stopped acceptance because Windows materialized the frozen Red test with CRLF even though its Git blob retained exact accepted SHA-256 `B783298F44BE7C104A498372746D69D5905254CB63C453B4CDAACF049FCAAC07`. The coordinator added only an exact test-path LF attribute; no test or product content changed. Review, a preserved correction commit, and a complete new proof remain.

2026-08-16 / Codex primary coordinator: Focused correction-loop review passed the exact frozen-test LF rule with no finding. The reviewer confirmed exact scope, unchanged generated rule, Windows checkout semantics, working/committed hashes, zero CRLF pairs, LF index/working identities, passing diff gates, and accurate evidence without changing the repository. The preserved correction commit and complete clean proof remain before Milestone 1 acceptance.

2026-08-16 / Codex primary coordinator: Created correction commit `aa4454ceed074540d35e48945b1ba671ea24900c` and completed the authorized exact detached clean proof under Windows `core.autocrlf=true`. Exact install, all five direct pins, schema drift, focused 5/5, affected 6/6, root typecheck, documentation/ADR validation, root build, diff and marker audits, exact commit identity, frozen test/generated hashes, and clean status pass. The primary accepts only Milestone 1; DPL-DEC-041 remains explicit post-MVP debt, separate Sequelize/uuid maintenance remains pre-production, TASK-006 remains `In progress`, and no AC or full NFR-003 status advances.

2026-08-16 / Codex primary coordinator: Accepted the fresh Milestone 2 test-owner preflight as `PARTIAL` at clean HEAD `d14cbc144ca4ed4c1029df229d8de1fcc299b6f8`. The existing GraphQL schema/resolver forwards all five optional inputs, and TASK-004 supplies a migrated Sequelize Character model and isolated PostgreSQL lifecycle; no normalization implementation, character search repository, production database composition, or focused filter evidence exists. The minimum coherent Red is one unit normalization/service contract plus one Express-to-real-PostgreSQL integration contract covering all five filters, blank omission, `AND`, empty results, literal `%`, `_`, quote and combined metacharacters, and exact stored `imageUrl`. Docker runs the task-owned Compose PostgreSQL instance on free loopback port 55432 because an unrelated local process owns 5432; that process remains untouched. No repository or database mutation occurred during the test-owner preflight. The guarded Red, namespace evidence, cleanup result, Green, review, commit, clean proof, and Milestone 2 acceptance remain pending.

2026-08-16 / Codex primary coordinator: Closed Red lease `TASK-006-20260816-02-M2-red-01` compliant after it created only the two authorized tests. API strict compilation passed, and the exact focused command reached a real migrated PostgreSQL namespace before exiting 1 across three tests for only the accepted gaps: two absent `createCharacterReadService` failures and `TASK_006_RUNTIME_COMPOSITION_MISSING`. Namespace `task_004_a0f9cf25124650f6` cleaned exactly with zero residue. Unit SHA-256 `04C0639B916061B28FE8295CF51BE64E289BF9971D7EF700FD14B29A57B3B41E` and integration SHA-256 `69F8E334D51D65F22872C98A8A72C0059E801E50BCA2ADB9C4C5F68D63B9CA61` are now frozen for Green. No production, existing-test, configuration, manifest, generated, or database-lifecycle path changed. Green, affected validation, critical review, exact-tree commit, clean proof, and Milestone 2 acceptance remain pending.

2026-08-16 / Codex primary coordinator: Closed attempt-1 Green lease `TASK-006-20260816-02-M2-green-01` compliant after it touched only the four authorized production paths. Focused real-PostgreSQL Green passed 3/3 with owned cleanup; API typecheck, schema drift, affected application 6/6, API build, frozen hashes, scope, and diff checks passed. The worker's built-server probe also returned `/healthz` with an unreachable database port when a credential was supplied. The primary did not accept Green because canonical credential-free `npm run test:smoke` failed at eager `POSTGRES_RUNTIME_CONFIG_INVALID` before the API could bind, and review of the application boundary found the repository port still consumed `CharacterFilterInput` rather than a distinct normalized type. The smoke process was stopped and ports 4173/4174 were verified free. One bounded same-contract code correction may defer production database configuration/pool creation until the first GraphQL read and align the normalized repository boundary; it may not change frozen tests or expand beyond the existing four production paths. Focused and every affected join must rerun before S3 review or milestone acceptance.

2026-08-16 / Project owner and Codex primary coordinator: Paused TASK-006 at the first safe overnight boundary after correction lease `TASK-006-20260816-02-M2-green-02` closed compliant. The correction separated normalized filters and deferred production database configuration/pool creation until the first GraphQL read. Focused PostgreSQL 3/3, strict typecheck, schema drift, affected application 6/6, API build, frozen hashes, and credential-free invalid-database liveness pass. Canonical smoke remains non-terminal: worker reproduction twice reached the only test, and primary Playwright diagnostics proved navigation, heading visibility, HTTP 200 `/healthz`, and browser closure before hanging at managed web-server termination. Exact task-owned sessions were interrupted and ports 3000/3107/4173/4174 are free. The final ordinary code-worker correction budget is exhausted; no direct primary code edit, critical review, commit of product/test paths, clean-checkout proof, or Milestone 2 acceptance is claimed. Before pausing infrastructure, the primary verified Compose project `rick-and-morty-dev`, PostgreSQL container `5586c564678c` on loopback 55432, Redis container `dc1597caf708`, and zero `task_004_*` database residue. Both exact services are stopped with exit 0 and their containers/volumes preserved for resume. All leases are closed-compliant; the six Milestone 2 product/test paths remain deliberately unstaged in the primary worktree. Resume by starting the same Compose project on port 55432, diagnosing the server/Playwright teardown boundary without changing behavior first, and then obtaining the workflow-authorized correction route before any further write.

2026-08-17 / Project owner and Codex primary coordinator: Resumed on the owner's `continua` direction, restarted only the preserved Compose project on loopback port 55432, and diagnosed the smoke stop without changing behavior. Playwright's sandboxed Windows teardown could not terminate its own server trees; the canonical smoke passed 1/1 and the lifecycle matrix passed 6/6 with the required owned-process permission. Focused PostgreSQL passed 3/3, API unit 56/56, API application 6/6, canonical root integration 68/68 with zero residue, root typecheck, and root build all passed. The primary added only exact LF rules for the two unchanged frozen tests. Fresh S3 review then returned `REVISE`: prohibited default ordering and a shutdown race around an in-flight first-read initializer are two Major findings; every other reviewed SQL, filter, URL, liveness, cleanup, and boundary property passed. Because regression coverage needs a new test-owner path and implementation correction exceeds the ordinary code-worker budget, no product/test edit, stage, commit, clean proof, or milestone acceptance follows without exact owner direction.

2026-08-17 / Codex primary coordinator: The persistent test owner performed a no-write correction preflight and classified only the in-flight shutdown ownership as `PARTIAL`. It proposed one deterministic API-unit Red at `apps/api/src/runtime-composition.unit.test.ts`, preserving both existing frozen tests, followed by an owner-authorized exceptional correction limited to `runtime-composition.ts`, `server.ts`, and `sequelize-character-read-repository.ts`. The Red controls a deferred initializer and proves shutdown waits for and closes a post-shutdown resource exactly once; existing smoke lifecycle supplies built-process termination evidence, so no platform-sensitive child-process test or real PostgreSQL mutation is needed. This exact route remains unexecuted pending owner authorization.

2026-08-17 / Project owner and Codex primary coordinator: The owner responded `Autorizo` to the exact correction request. Authority now permits only one guarded Red at `apps/api/src/runtime-composition.unit.test.ts`, followed after Red acceptance by one exhausted-budget exceptional implementation correction at `runtime-composition.ts`, `server.ts`, and `sequelize-character-read-repository.ts`, plus a primary exact-path LF rule for the new frozen test if required. Existing frozen tests, dependencies, configuration, migrations, unrelated behavior, staging/commit ownership, and external-publication restrictions remain unchanged. No worker write began before this authority record and documentation validation.

2026-08-17 / Codex primary coordinator: Accepted Red lease `TASK-006-20260816-02-M2-red-02-scope-only` as compliant from receipt digest `b88f13b15ba8a23d5c3122200e95d32d6c4d4db5fa7064cb262dffa4d08d147b`. It created only the authorized deterministic unit test. Root typecheck passed; the exact focused command discovered one file and one scenario and exited 1 only because `createLazyCharacterReadServiceOwner` is absent. Frozen SHA-256 is `CDAC325DCDC0361BE7F0C4736EB8D3F513692DCF4A0E6C2B5AD9A1A239BBBF42`; both prior M2 frozen hashes remain exact. Between leases, the primary added only the authorized exact-path `text eol=lf` rule, verified the effective attributes, and re-confirmed the unchanged hash. The one owner-directed exceptional Green may now begin on only the three named product paths.

2026-08-17 / Codex primary coordinator: Closed exceptional Green lease `TASK-006-20260816-02-M2-green-02-owner-exception` compliant with receipt digest `8e7d499e42c36e95be29d9c5f8dfa796bb883fc4a7f722e6a823b79d0f97d8e1`. Only `runtime-composition.ts`, `server.ts`, and `sequelize-character-read-repository.ts` changed: the lazy owner now closes a resource whose initialization resolves after shutdown begins, production composition uses that owner, and default SQL ordering is removed. Exact correction Red passed 1/1; API unit passed 57/57; focused real PostgreSQL passed 3/3; API application passed 6/6; the authoritative repeated canonical integration run passed 68/68 and left zero `task_004_*` databases; root strict typecheck, schema drift, and build passed; credential-free smoke passed 1/1; lifecycle verification passed 6/6; ports 4173/4174 are free; and all three frozen hashes remain exact. One earlier canonical integration invocation also completed and cleaned to zero but lost its terminal result at the command-capture boundary, so it is not acceptance evidence and the identical terminal repetition is authoritative. No review, stage, commit, clean-checkout proof, or Milestone 2 acceptance is yet claimed.

2026-08-17 / Codex primary coordinator: Bounded S3 correction review found no remaining product defect and returned `REVISE` only because the living Outcomes paragraph still described Milestones 2 through 4 as unstarted. The reviewer independently confirmed both prior Majors closed, all listed SQL/lifecycle/liveness/boundary properties, exact frozen hashes, and LF scope on relevant-tree fingerprint `9816A2B24D05999DAFB899E29DAB0343806E8A77CC8588330BD023908BAE42F1`. The primary changed only the stale current-state sentence to distinguish Milestone 2's joined but unaccepted Green candidate from unstarted Milestones 3 and 4, and recorded the review without rewriting historical chronology. Documentation validation and bounded confirmation remain required before commit or acceptance.

2026-08-17 / Codex primary coordinator: The same read-only S3 reviewer returned bounded documentation-confirmation `PASS` with no Blocker, Major, or Minor. It confirmed the corrected Outcomes state, accurate Progress and Revision Note, append-only execution-log preservation, unchanged product/test candidate and frozen hashes, and passing documentation/diff evidence. This closes the review loop but does not itself accept Milestone 2; exact primary staging, local commit, authorized clean-checkout proof, evidence reconciliation, and acceptance remain pending.

2026-08-17 / Codex primary coordinator: Created exact reviewed local commit `563875cf76925a1a5e790b96d3721d699759c6b1` and accepted Milestone 2 after its authorized detached Windows clean proof passed. The proof used Node 24.18.0, npm 11.16.0, `core.autocrlf=true`, exact lockfile installation and direct pins, schema drift, correction unit 1/1, focused PostgreSQL 3/3, API unit 57/57, API application 6/6, canonical integration 68/68 with zero residue, root typecheck/build, canonical credential-free smoke 1/1, lifecycle 6/6, documentation/ADR validation, diff/marker audits, all frozen hashes with zero CRLF pairs, exact commit identity, zero listeners, and clean status. An initial API-unit invocation run in parallel with build/application failed one unrelated migration-artifact test at a Windows Vite-cache `EPERM` rename; the identical isolated rerun passed without repository change, so the isolated terminal result is authoritative. This accepts only Milestone 2; TASK-006 remains `In progress`, no canonical AC or full NFR-003 claim advances, and Milestones 3 and 4 remain unstarted.

2026-08-17 / Codex primary coordinator: Accepted the read-only Milestone 3 preflight's `UNKNOWN` classification because both the all-Express request logger and its promised exact serialization decision were absent. After re-listing DPL-DEC-001 through DPL-DEC-041, allocated DPL-DEC-042 for one fixed seven-field JSON-plus-LF record: server-owned bounded request ID, method, query-free path, response status, monotonic bounded duration, safely observed bounded GraphQL error count, and bounded selected operation name or exact `null`. String normalization and byte limits keep the whole UTF-8 line at or below 1024 bytes while excluding headers, bodies, query strings, GraphQL documents/variables, secrets, stacks, infrastructure detail, and diagnostics. This resolves only the reversible test contract; no Red, product implementation, dependency, route, configuration, runtime evidence, or acceptance result exists yet.

2026-08-17 / Codex primary coordinator: Accepted the corrected Milestone 3 Red after both guarded test-owner leases closed compliant. Attempt 1 passed strict typecheck and completed health, GraphQL success, and GraphQL validation failure before the one coherent test failed solely because zero request records were emitted; it was not frozen because short operation names did not test the DPL-DEC-042 bound. Attempt 2 changed only the successful fixture to a valid 160-byte operation name and its required 96-byte projection. Strict typecheck passed again; the exact focused run completed all three requests, including the long-name HTTP 200 response, and exited 1 only at unchanged marker `TASK_006_REQUEST_LOG_RECORD_MISSING`. Frozen SHA-256 is `A0B75EBDA092064A485213FCCD97C511081C046D273F437CF8911883739533C4`; production implementation, affected validation, critical review, commit, and Milestone 3 acceptance remain pending.

2026-08-17 / Codex primary coordinator: Closed attempt-1 Milestone 3 Green lease compliant after it changed only the four authorized source paths. Focused request logging passed 1/1 and affected API application passed 7/7 after capturing the Express path before route mounting. The fixed test hash remains exact, and production composition supplies standard output, `randomUUID`, and `performance.now`. Root typecheck nevertheless reports only five new boundary diagnostics: the Yoga callable overload in `app.ts`, implicit plugin-hook parameters, and lost custom server context in `graphql-handler.ts`. The worker stopped before later joins as required. Local declaration inspection isolates a behavior-preserving `.handle`/explicit-plugin-generic correction inside the same four paths; one ordinary attempt-2 Green remains, while tests, compiler settings, dependencies, schema, docs, and all other behavior remain frozen.

2026-08-17 / Codex primary coordinator: Closed the sole ordinary Milestone 3 Green correction lease compliant. Explicit `Plugin` generics resolve all four Yoga hook-context errors; strict typecheck now reports one remaining TS2769 in `app.ts` because Express `Request` cannot select the generic `.handle` overload. The worker correctly stopped with no behavior rerun after the type change. A no-write TypeScript-program probe against the exact current API tree replaced only the adapter call with `handleNodeRequestAndResponse` and conditionally omitted absent request-log metadata under `exactOptionalPropertyTypes`; it returned zero diagnostics. The ordinary budget is exhausted, so that exact one-file behavior-preserving correction remains unapplied pending project-owner authorization. All leases are terminal-compliant, no task process/service is running, HEAD/index are unchanged, and the frozen Red hash remains exact.

2026-08-17 / Project owner and Codex primary coordinator: The owner responded `autorizo` to the exact one-file exception request. This authorizes the primary to change only `apps/api/src/app.ts`, replace Yoga's nonselectable generic `.handle` call with the typed Node request/response adapter, and omit the optional metadata property when logging is absent. It authorizes no further test or worker correction, no source/path/behavior expansion, no dependency or compiler exception, and no acceptance claim. The edit must match the zero-diagnostic in-memory probe and every stale behavior, type, schema, unit, build, integration, smoke, and review gate must pass afterward.

2026-08-17 / Codex primary coordinator: Applied the exact authorized `app.ts` edit directly. Root typecheck passed, but the first focused behavior command timed out because `handleNodeRequestAndResponse` returns a Fetch response without invoking the adapter's Node send path. The primary restored the prior `.handle` candidate exactly and did not continue validation. Installed source inspection confirms the generic request handler routes Node requests through `requestListener`, which calls both `handleNodeRequestAndResponse` and `sendNodeResponse`. Read-only strict probes then established that Express request/response types are compatible with `.handle`; only the undefined-valued optional metadata object prevents overload selection. Retaining `.handle` and conditionally passing `{}` or `{ requestLogMetadata }` compiles with zero diagnostics and preserves the already passing runtime path. Because the explicitly authorized method changed, that revised one-file correction remains unapplied pending owner direction; no technical debt or broader fix is proposed.

2026-08-17 / Project owner and Codex primary coordinator: The owner granted standing authority for bounded corrections required by the remainder of TASK-006 and permitted only non-blocking, future-safe issues to be recorded for possible post-MVP work with `Critical`, `Medium`, or `Low` severity. This authority covers the already isolated `app.ts` `.handle` exact-optional correction and future same-contract exceptions after normal workflow stops, but it does not reset ordinary attempt budgets, widen requirements, waive mandatory/security/correctness/closure findings, or authorize push, PR, deployment, or live-upstream activity. Each exceptional edit still records its exact path, reason, and validation. Critical debt requires case-specific owner review; every deferred item needs a rationale and exit criterion. The canonical deferred-follow-up section now classifies the existing DPL-DEC-041 compatibility exception as `Medium`; the separately scoped Sequelize/uuid advisories remain pre-production maintenance and are not silently deferred.

2026-08-17 / Codex primary coordinator: Applied only the standing-authorized `apps/api/src/app.ts` correction, retaining Yoga's Node-sending `.handle` route and conditionally omitting absent request-log metadata under `exactOptionalPropertyTypes`. Root typecheck, focused request logging 1/1, affected API application 7/7, schema drift, repeated API unit 57/57, root build, focused real-PostgreSQL 1/1, canonical integration 68/68 with zero temporary-database residue, credential-free smoke 1/1, all three smoke-lifecycle scenarios, frozen test SHA-256 `A0B75EBDA092064A485213FCCD97C511081C046D273F437CF8911883739533C4`, and diff checking pass. The first API-unit aggregate attempt failed only the historical TASK-004 default-entry observation group; its exact isolated test and the identical full suite passed without a source or environment correction, so only the repeated full-suite result is used and the transient remains explicit review input. Production smoke emitted the required bounded `/healthz` JSON records to standard output, owned ports are free, zero `task_004_*` databases remain, and only the task-owned Compose services were stopped while containers and volumes were preserved. Milestone 3 remains unaccepted until fresh S3 review and exact committed proof.

2026-08-17 / Codex primary coordinator: Fresh S3 critical review returned `REVISE` with one Major and no Blocker or Minor on fingerprint `7E5358AD4CE71C8FB34DABF6520A9032C6EF7E56FB4E25C1766ACF38AA9FB39D`. Independent inspection and in-memory/ephemeral probes passed global finish semantics, exactly-one output, request-local concurrency, parse/selection/execution error counts, safe operation selection, key order, LF, C0/C1 and lone-surrogate normalization, scalar-safe clipping, numeric bounds, a 946-byte worst-case escaped record, exclusion safety, production stdout/UUID/monotonic wiring, liveness, and scope. A throwing injected writer propagates from `finish`, but DPL-DEC-042 defines no sink-failure isolation, so the review correctly made no fail-open claim and no finding. The sole Major is repository identity: `core.autocrlf=true` plus unspecified attributes can rematerialize the frozen Red with CRLF in a clean Windows checkout. Under standing owner authority, the primary will add only the exact test-path `text eol=lf` rule, preserve all product/test bytes, validate its effect, and request bounded confirmation before commit or acceptance. The transient TASK-004 unit observation needs no debt entry because unchanged isolated and aggregate repetitions passed.

2026-08-17 / Codex primary coordinator: Applied the sole review correction by adding `apps/api/src/transport/http/request-log.application.test.ts text eol=lf` to `.gitattributes`, matching the established exact-path frozen-test policy. Effective attributes now report `text: set` and `eol: lf`; the working file has zero CRLF pairs and unchanged SHA-256 `A0B75EBDA092064A485213FCCD97C511081C046D273F437CF8911883739533C4`; and `git diff --check` passes. No product, test, dependency, schema, generated, configuration-runtime, or external-state byte changed. The same S3 reviewer must confirm this bounded correction before primary staging and the exact Windows clean proof.

2026-08-17 / Codex primary coordinator: The same read-only S3 reviewer returned bounded correction-confirmation `PASS` with no Blocker, Major, or Minor. It independently confirmed exact-path scope, effective `text eol=lf`, unchanged default/neighboring whitespace behavior, zero frozen-test CRLF pairs, the exact frozen hash, unchanged five-member product/test hash set, accurate append-only review/correction records, the explicit sink-failure nonclaim, and no debt entry for the resolved transient unit observation. Documentation and ADR validators and diff checking pass; no validator artifact appeared. Current eight-path fingerprint is `632108775EEC7B1798B3E72E873DA2EEDAA62BD2C8C5DC3EB56F79C9A433ECD6`. The primary candidate commit and exact Windows clean proof remain before acceptance.

2026-08-17 / Codex primary coordinator: Created exact reviewed local commit `14ae00b40ce915e784535a0e78e4cc7f1271e9e8` (`feat: add TASK-006 bounded request logging`) from the nine-path index with no unstaged change, then completed the required detached Windows identity proof. Post-commit root typecheck and focused logging 1/1 pass. The earlier exact-tree affected application 7/7, schema drift, repeated API unit 57/57, root build, focused PostgreSQL 1/1, canonical integration 68/68 with zero residue, smoke 1/1, lifecycle, cleanup, validators, and diff evidence remains reusable because the subsequent correction and commit changed no product or test byte. Detached checkout `C:\Users\mmjos\AppData\Local\Temp\rick-and-morty-task006-m3-14ae00b` at the exact commit and Windows `core.autocrlf=true` reported `i/lf w/lf attr/text eol=lf`, zero CRLF pairs, exact frozen hash `A0B75EBDA092064A485213FCCD97C511081C046D273F437CF8911883739533C4`, passing commit diff/documentation/ADR checks, exact HEAD, and clean status. The verified checkout was removed. The primary accepts only Milestone 3; TASK-006 stays `In progress`, AC-011 remains an evidence candidate rather than canonical `Pass`, and owner reconciliation still blocks Milestone 4.
